<UserControl x:Class="ProductionManagement.WPF.Views.Items.ItemListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:ProductionManagement.WPF.ViewModels.Items"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ItemListViewModel}"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="品目マスタ"
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="新規登録"
                        Command="{Binding CreateCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0"/>
            </StackPanel>
        </DockPanel>

        <!-- 検索フォーム -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- キーワード検索 -->
                <TextBox Grid.Column="0"
                         materialDesign:HintAssist.Hint="品目コードまたは品名で検索"
                         Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,16,0">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <!-- 品目区分フィルター -->
                <ComboBox Grid.Column="1"
                          materialDesign:HintAssist.Hint="品目区分"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          Margin="0,0,16,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource EnumDisplayNameConverter}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- 検索ボタン -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="検索"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                            Margin="0,0,8,0"/>
                    <Button Content="クリア"
                            Command="{Binding ClearFilterCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- 品目一覧 DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">

            <!-- ダブルクリックで詳細表示 -->
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDoubleClick">
                    <b:InvokeCommandAction Command="{Binding RowDoubleClickCommand}"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>

            <DataGrid.Columns>
                <DataGridTextColumn Header="品目コード"
                                    Binding="{Binding ItemCode}"
                                    Width="120"/>
                <DataGridTextColumn Header="品名"
                                    Binding="{Binding ItemName}"
                                    Width="*"/>
                <DataGridTextColumn Header="品目区分"
                                    Binding="{Binding ItemCategory, Converter={StaticResource EnumDisplayNameConverter}}"
                                    Width="100"/>
                <DataGridTextColumn Header="リードタイム"
                                    Binding="{Binding LeadTime, StringFormat={}{0} 日}"
                                    Width="100"/>
                <DataGridTextColumn Header="安全在庫"
                                    Binding="{Binding SafetyStock}"
                                    Width="100"/>
                <DataGridTemplateColumn Header="操作" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="詳細"
                                        Command="{Binding DataContext.ShowDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Margin="0,0,4,0"/>
                                <Button Content="削除"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource MaterialDesignFlatSecondaryButton}"
                                        Foreground="Red"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ステータスバー -->
        <Border Grid.Row="3" Padding="8" Background="{DynamicResource MaterialDesignPaper}">
            <DockPanel>
                <TextBlock Text="{Binding Items.Count, StringFormat=全 {0} 件}"
                           DockPanel.Dock="Left"/>
                <ProgressBar Width="100" Height="4"
                             IsIndeterminate="{Binding IsLoading}"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                             HorizontalAlignment="Right"/>
            </DockPanel>
        </Border>

        <!-- ローディングオーバーレイ -->
        <Grid Grid.RowSpan="4"
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True"
                         Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>
