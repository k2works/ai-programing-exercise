<UserControl x:Class="ProductionManagement.WPF.Views.PurchaseOrders.PurchaseOrderListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:vm="clr-namespace:ProductionManagement.WPF.ViewModels.PurchaseOrders"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PurchaseOrderListViewModel}"
             d:DesignHeight="600" d:DesignWidth="1200">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="発注一覧"
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       VerticalAlignment="Center"/>
            <Button Content="新規登録"
                    Command="{Binding CreateCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    HorizontalAlignment="Right"/>
        </DockPanel>

        <!-- 検索フォーム -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <!-- 日付範囲 -->
                    <DatePicker SelectedDate="{Binding DateFrom}"
                                Width="130"
                                materialDesign:HintAssist.Hint="発注日（開始）"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                Margin="0,0,8,0"/>
                    <TextBlock Text="～" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <DatePicker SelectedDate="{Binding DateTo}"
                                Width="130"
                                materialDesign:HintAssist.Hint="発注日（終了）"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                                Margin="0,0,16,0"/>

                    <!-- ステータス -->
                    <ComboBox ItemsSource="{Binding Statuses}"
                              SelectedItem="{Binding SelectedStatus}"
                              Width="150"
                              materialDesign:HintAssist.Hint="ステータス"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              Margin="0,0,16,0"/>

                    <!-- 取引先コード -->
                    <TextBox Text="{Binding SupplierCode, UpdateSourceTrigger=PropertyChanged}"
                             Width="150"
                             materialDesign:HintAssist.Hint="取引先コード"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,16,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding LoadCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <!-- ボタン -->
                    <Button Content="検索"
                            Command="{Binding LoadCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,8,0"/>
                    <Button Content="クリア"
                            Command="{Binding ClearFilterCommand}"
                            Style="{StaticResource MaterialDesignOutlinedSecondaryButton}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <!-- データグリッド -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Orders}"
                  SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  Style="{StaticResource MaterialDesignDataGrid}">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseDoubleClick">
                    <b:InvokeCommandAction Command="{Binding RowDoubleClickCommand}"
                                           CommandParameter="{Binding SelectedOrder}"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>
            <DataGrid.Columns>
                <DataGridTextColumn Header="発注番号" Binding="{Binding PurchaseOrderNumber}" Width="140"/>
                <DataGridTextColumn Header="発注日" Binding="{Binding OrderDate, StringFormat=yyyy/MM/dd}" Width="100"/>
                <DataGridTextColumn Header="取引先コード" Binding="{Binding SupplierCode}" Width="120"/>
                <DataGridTemplateColumn Header="ステータス" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Status, Converter={StaticResource EnumDisplayNameConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="備考" Binding="{Binding Remarks}" Width="200"/>
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="編集"
                                        IsEnabled="{Binding DataContext.CanEdit, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                    <materialDesign:PackIcon Kind="Pencil"/>
                                </Button>
                                <Button Command="{Binding DataContext.ConfirmCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="確定"
                                        Foreground="Green">
                                    <materialDesign:PackIcon Kind="Check"/>
                                </Button>
                                <Button Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="取消"
                                        Foreground="Red">
                                    <materialDesign:PackIcon Kind="Cancel"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- フッター（件数表示） -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="件数: "/>
            <TextBlock Text="{Binding Orders.Count}"/>
            <TextBlock Text=" 件"/>
        </StackPanel>

        <!-- ローディング -->
        <Grid Grid.RowSpan="4"
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True"
                         Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>
