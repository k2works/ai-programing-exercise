<UserControl x:Class="ProductionManagement.WPF.Views.PurchaseOrders.PurchaseOrderEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:ProductionManagement.WPF.ViewModels.PurchaseOrders"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PurchaseOrderEditViewModel}"
             d:DesignHeight="700" d:DesignWidth="1000">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <Button DockPanel.Dock="Left"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignIconButton}"
                    ToolTip="戻る">
                <materialDesign:PackIcon Kind="ArrowLeft"/>
            </Button>
            <TextBlock Text="{Binding Title}"
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       VerticalAlignment="Center"
                       Margin="8,0,0,0"/>
            <TextBlock Text="{Binding PurchaseOrderNumber}"
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       VerticalAlignment="Center"
                       Margin="16,0,0,0"
                       Foreground="Gray"
                       Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </DockPanel>

        <!-- ヘッダー情報 -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 発注日 -->
                <DatePicker Grid.Column="0"
                            SelectedDate="{Binding OrderDate}"
                            materialDesign:HintAssist.Hint="発注日 *"
                            Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                            IsEnabled="{Binding CanEditHeader}"
                            Margin="0,0,16,0"/>

                <!-- 取引先 -->
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding Suppliers}"
                          SelectedItem="{Binding SelectedSupplier}"
                          materialDesign:HintAssist.Hint="取引先 *"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          IsEnabled="{Binding CanEditHeader}"
                          Margin="0,0,16,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding SupplierCode}" Width="80"/>
                                <TextBlock Text="{Binding SupplierName}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- 備考 -->
                <TextBox Grid.Column="2"
                         Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="備考"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
            </Grid>
        </materialDesign:Card>

        <!-- 明細ヘッダー -->
        <DockPanel Grid.Row="2" Margin="0,0,0,8">
            <TextBlock Text="明細"
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="行追加"
                        Command="{Binding AddDetailCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Margin="0,0,8,0">
                    <Button.IsEnabled>
                        <Binding Path="CanEditHeader"/>
                    </Button.IsEnabled>
                </Button>
                <Button Content="行削除"
                        Command="{Binding RemoveDetailCommand}"
                        Style="{StaticResource MaterialDesignOutlinedSecondaryButton}">
                    <Button.IsEnabled>
                        <MultiBinding>
                            <MultiBinding.Converter>
                                <local:AllTrueConverter xmlns:local="clr-namespace:ProductionManagement.WPF.Converters"/>
                            </MultiBinding.Converter>
                            <Binding Path="CanEditHeader"/>
                            <Binding Path="SelectedDetail" Converter="{StaticResource NullToVisibilityConverter}"/>
                        </MultiBinding>
                    </Button.IsEnabled>
                </Button>
            </StackPanel>
        </DockPanel>

        <!-- 明細グリッド -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Details}"
                  SelectedItem="{Binding SelectedDetail}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  Style="{StaticResource MaterialDesignDataGrid}"
                  Margin="0,40,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="行" Binding="{Binding LineNumber}" Width="50" IsReadOnly="True"/>

                <!-- 品目 -->
                <DataGridTemplateColumn Header="品目 *" Width="250">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} - {1}">
                                        <Binding Path="SelectedItem.ItemCode"/>
                                        <Binding Path="SelectedItem.ItemName"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.Items, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                      SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}"
                                      IsEditable="True"
                                      Style="{StaticResource MaterialDesignComboBox}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding ItemCode}" Width="80"/>
                                            <TextBlock Text="{Binding ItemName}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- 数量 -->
                <DataGridTextColumn Header="数量 *" Binding="{Binding OrderQuantity, StringFormat=N2}" Width="100"/>

                <!-- 単価 -->
                <DataGridTextColumn Header="単価" Binding="{Binding OrderUnitPrice, StringFormat=N2}" Width="100"/>

                <!-- 金額 -->
                <DataGridTextColumn Header="金額" Binding="{Binding OrderAmount, StringFormat=N0}" Width="120" IsReadOnly="True"/>

                <!-- 納期 -->
                <DataGridTemplateColumn Header="納期 *" Width="130">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ExpectedReceivingDate, StringFormat=yyyy/MM/dd}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <DatePicker SelectedDate="{Binding ExpectedReceivingDate, UpdateSourceTrigger=PropertyChanged}"
                                        Style="{StaticResource MaterialDesignDatePicker}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <!-- 備考 -->
                <DataGridTextColumn Header="明細備考" Binding="{Binding DetailRemarks}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 合計 -->
        <materialDesign:Card Grid.Row="3" Padding="16" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="合計金額:"
                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                           VerticalAlignment="Center"
                           Margin="0,0,16,0"/>
                <TextBlock Text="{Binding TotalAmount, StringFormat=C0}"
                           Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                           VerticalAlignment="Center"
                           FontWeight="Bold"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- ボタン -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="キャンセル"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,16,0"/>
            <Button Content="保存"
                    Command="{Binding SaveCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}">
                <Button.IsEnabled>
                    <Binding Path="CanEditHeader"/>
                </Button.IsEnabled>
            </Button>
        </StackPanel>

        <!-- ローディング -->
        <Grid Grid.RowSpan="5"
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         IsIndeterminate="True"
                         Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>
