<UserControl x:Class="ProductionManagement.WPF.Views.Grpc.GrpcBomExplodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:ProductionManagement.WPF.ViewModels.Grpc"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:GrpcBomExplodeViewModel}"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="BOM 展開 (gRPC)"
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
            <TextBlock Text="正展開（Server Streaming）と逆展開（Unary）"
                       Style="{StaticResource MaterialDesignCaptionTextBlock}"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- 操作パネル -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0"
                          ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem}"
                          materialDesign:HintAssist.Hint="品目を選択"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          Margin="0,0,8,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding ItemCode}" Width="100"/>
                                <TextBlock Text="{Binding ItemName}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button Grid.Column="1"
                        Command="{Binding ExplodeCommand}"
                        Content="正展開"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0"/>

                <Button Grid.Column="2"
                        Command="{Binding WhereUsedCommand}"
                        Content="逆展開"
                        Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                        Margin="0,0,8,0"/>

                <Button Grid.Column="3"
                        Command="{Binding ClearCommand}"
                        Content="クリア"
                        Style="{StaticResource MaterialDesignOutlinedButton}"/>
            </Grid>
        </materialDesign:Card>

        <!-- 結果表示エリア -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 正展開ツリー -->
            <GroupBox Grid.Column="0" Header="正展開結果" Margin="0,0,8,0">
                <TreeView ItemsSource="{Binding BomTree}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding ItemCode}" FontWeight="Bold" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding ItemName}" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding RequiredQuantity.Value, StringFormat=数量: {0}}"
                                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </GroupBox>

            <!-- 逆展開結果 -->
            <GroupBox Grid.Column="1" Header="逆展開結果" Margin="8,0,0,0">
                <DataGrid ItemsSource="{Binding WhereUsedResults}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="親品目コード" Binding="{Binding ParentItemCode}" Width="*"/>
                        <DataGridTextColumn Header="品目名" Binding="{Binding ItemName}" Width="*"/>
                        <DataGridTextColumn Header="所要量" Binding="{Binding RequiredQuantity.Value}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>

        <!-- ステータスバー -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StreamingStatus}"
                           VerticalAlignment="Center"
                           Margin="0,0,16,0"/>
                <ProgressBar IsIndeterminate="{Binding IsLoading}"
                             Width="100"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
