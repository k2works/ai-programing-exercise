server:
  port: 8082

spring:
  application:
    name: management-accounting-service

# Financial Accounting Service の URL
financial-accounting-service:
  base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.management: DEBUG
    org.springframework.web: INFO
