(ns algorithm-clj.algorithms.sorting
  "ソートアルゴリズムの実装")

;; 今後、以下のようなソートアルゴリズムを実装予定：
;; - バブルソート
;; - 選択ソート
;; - 挿入ソート
;; - マージソート
;; - クイックソート

(defn bubble-sort
  "バブルソートアルゴリズムの実装（今後実装予定）"
  [coll]
  ;; TODO: 実装予定
  coll)
