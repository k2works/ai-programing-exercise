<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.production.infrastructure.out.mapper.OrderMapper">

    <resultMap id="OrderResultMap" type="com.example.production.domain.model.plan.Order">
        <id property="id" column="ID"/>
        <result property="orderNumber" column="オーダNO"/>
        <result property="orderType" column="オーダ種別"
                typeHandler="com.example.production.infrastructure.out.OrderTypeTypeHandler"/>
        <result property="itemCode" column="品目コード"/>
        <result property="startDate" column="着手予定日"/>
        <result property="dueDate" column="納期"/>
        <result property="expirationDate" column="有効期限"/>
        <result property="planQuantity" column="計画数量"/>
        <result property="locationCode" column="場所コード"/>
        <result property="status" column="ステータス"
                typeHandler="com.example.production.infrastructure.out.PlanStatusTypeHandler"/>
        <result property="mpsId" column="MPS_ID"/>
        <result property="parentOrderId" column="親オーダID"/>
        <result property="createdAt" column="作成日時"/>
        <result property="createdBy" column="作成者"/>
        <result property="updatedAt" column="更新日時"/>
        <result property="updatedBy" column="更新者"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.production.domain.model.plan.Order"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID">
        INSERT INTO "オーダ情報" (
            "オーダNO",
            "オーダ種別",
            "品目コード",
            "着手予定日",
            "納期",
            "有効期限",
            "計画数量",
            "場所コード",
            "ステータス",
            "MPS_ID",
            "親オーダID",
            "作成日時",
            "作成者",
            "更新日時",
            "更新者"
        ) VALUES (
            #{orderNumber},
            #{orderType, typeHandler=com.example.production.infrastructure.out.OrderTypeTypeHandler}::オーダ種別,
            #{itemCode},
            #{startDate},
            #{dueDate},
            #{expirationDate},
            #{planQuantity},
            #{locationCode},
            #{status, typeHandler=com.example.production.infrastructure.out.PlanStatusTypeHandler}::計画ステータス,
            #{mpsId},
            #{parentOrderId},
            CURRENT_TIMESTAMP,
            #{createdBy},
            CURRENT_TIMESTAMP,
            #{updatedBy}
        )
    </insert>

    <select id="findById" resultMap="OrderResultMap">
        SELECT * FROM "オーダ情報"
        WHERE "ID" = #{id}
    </select>

    <select id="findByOrderNumber" resultMap="OrderResultMap">
        SELECT * FROM "オーダ情報"
        WHERE "オーダNO" = #{orderNumber}
    </select>

    <select id="findByMpsId" resultMap="OrderResultMap">
        SELECT * FROM "オーダ情報"
        WHERE "MPS_ID" = #{mpsId}
        ORDER BY "オーダNO"
    </select>

    <select id="findByParentOrderId" resultMap="OrderResultMap">
        SELECT * FROM "オーダ情報"
        WHERE "親オーダID" = #{parentOrderId}
        ORDER BY "オーダNO"
    </select>

    <update id="updateParentOrderId">
        UPDATE "オーダ情報" SET
            "親オーダID" = #{parentOrderId},
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "ID" = #{id}
    </update>

    <update id="updateStatus">
        UPDATE "オーダ情報" SET
            "ステータス" = #{status, typeHandler=com.example.production.infrastructure.out.PlanStatusTypeHandler}::計画ステータス,
            "更新日時" = CURRENT_TIMESTAMP
        WHERE "ID" = #{id}
    </update>

    <select id="findAll" resultMap="OrderResultMap">
        SELECT * FROM "オーダ情報"
        ORDER BY "オーダNO"
    </select>

    <delete id="deleteAll">
        TRUNCATE TABLE "オーダ情報" CASCADE
    </delete>
</mapper>
