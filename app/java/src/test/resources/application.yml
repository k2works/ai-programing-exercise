spring:
  application:
    name: production-management

mybatis:
  configuration:
    map-underscore-to-camel-case: false

# テスト環境では gRPC サーバーを無効化（port: -1）
# gRPC テストでは in-process サーバーを使用
grpc:
  server:
    port: -1
