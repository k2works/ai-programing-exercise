---
title: システムユースケース - React Job Board Application
description: 求人掲載・管理システムのシステムユースケース仕様
published: true
date: 2025-09-11T05:00:00.000Z
tags: job-board, system-usecase, technical-specification
editor: markdown
dateCreated: 2025-09-11T05:00:00.000Z
---

# システムユースケース - React Job Board Application

## UC001: 🖥️求人検索実行🌊

**使用時のコンテキスト**: ユーザーが求人検索画面で検索条件を入力し、検索を実行する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 求職者

**利害関係者と利益**:
- **求職者**: 希望条件に一致する求人一覧を取得
- **システム**: 効率的な検索結果提供とユーザー体験向上

**事前条件**:
- 求人データベースにアクセス可能
- 検索インデックスが最新状態
- フロントエンドアプリケーションが稼働中

**最低保証**: 検索結果（該当なしを含む）が適切にレスポンスされる

**成功保証時**: 検索条件に一致する求人一覧が構造化されたデータで返される

**トリガー**: GET /api/jobs?query={searchParams} API呼び出し

**主成功シナリオ**:
1. システムがクエリパラメータを受信
2. システムが検索条件をバリデーション（必須項目、データ型）
3. システムがデータベースクエリを構築
4. システムがページネーション設定を適用
5. システムが求人情報を検索実行
6. システムが結果をJSON形式でシリアライズ
7. システムがHTTP 200とともに検索結果を返却

**拡張**:
- 2a バリデーションエラー: HTTP 400 Bad Request を返却
- 5a 検索結果0件: 空配列とtotalCount=0を返却
- 5b データベース接続エラー: HTTP 500 Internal Server Error を返却
- 6a 大量結果の場合: ページネーションメタデータを含む

**技術およびデータのバリエーションリスト**:
- 検索パラメータ: position, department, location, organizationId
- ソート条件: createdAt desc, position asc
- レスポンス形式: { data: Job[], total: number, page: number, limit: number }

**関連情報**:
- API エンドポイント: GET /api/jobs
- React Sample 参考: features/jobs/api/get-jobs.ts
- パフォーマンス要件: 1秒以内の応答時間

---

## UC002: 🖥️求人詳細取得🌊

**使用時のコンテキスト**: ユーザーが特定求人の詳細情報を要求する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 求職者

**利害関係者と利益**:
- **求職者**: 求人の完全な詳細情報を取得
- **採用企業**: 求人内容の正確な伝達

**事前条件**:
- 指定された求人IDが存在する
- 求人が公開状態である

**最低保証**: 有効な求人詳細または適切なエラーレスポンス

**成功保証時**: 完全な求人詳細情報がJSON形式で返される

**トリガー**: GET /api/jobs/{jobId} API呼び出し

**主成功シナリオ**:
1. システムが求人IDパラメータを受信
2. システムが求人IDの形式をバリデーション
3. システムがデータベースから求人詳細を取得
4. システムが求人の公開状態を確認
5. システムが関連する組織情報を取得
6. システムが統合された求人詳細を構築
7. システムがHTTP 200とともに詳細情報を返却

**拡張**:
- 2a 無効なID形式: HTTP 400 Bad Request を返却
- 3a 求人が存在しない: HTTP 404 Not Found を返却
- 4a 求人が非公開: HTTP 403 Forbidden を返却
- 5a 組織情報取得失敗: 基本的な組織情報のみ含む

**技術およびデータのバリエーションリスト**:
- URL パラメータ: jobId（UUID形式）
- レスポンス: Job オブジェクト + Organization 情報
- キャッシュ: Redis による5分間キャッシュ

**関連情報**:
- API エンドポイント: GET /api/jobs/:id
- データモデル: Job, Organization エンティティ
- セキュリティ: 非公開求人のアクセス制御

---

## UC003: 🖥️ユーザー認証🌊

**使用時のコンテキスト**: ユーザーがシステムにログインまたは新規登録を行う

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: システムユーザー（求職者・採用担当者・管理者）

**利害関係者と利益**:
- **ユーザー**: セキュアなアカウントアクセス
- **システム**: 適切な権限管理とセキュリティ保持

**事前条件**:
- 認証システムが稼働中
- JWT秘密鍵が設定済み

**最低保証**: 認証結果（成功/失敗）が適切に返される

**成功保証時**: 有効なJWTトークンとユーザー情報が返される

**トリガー**: POST /api/auth/login API呼び出し

**主成功シナリオ**:
1. システムがログイン認証情報を受信（email, password）
2. システムが入力データをバリデーション
3. システムがハッシュ化されたパスワードでユーザーを認証
4. システムがユーザーの権限情報を取得
5. システムがJWTトークンを生成
6. システムがHTTP 200とともにトークンとユーザー情報を返却

**拡張**:
- 2a バリデーションエラー: HTTP 400 Bad Request を返却
- 3a 認証失敗: HTTP 401 Unauthorized を返却
- 3b アカウントロック: HTTP 423 Locked を返却
- 5a トークン生成失敗: HTTP 500 Internal Server Error を返却

**技術およびデータのバリエーションリスト**:
- 認証方法: Email/Password, OAuth (Google, LinkedIn)
- トークン形式: JWT (有効期限: 24時間)
- レスポンス: { user: AuthUser, token: string }

**関連情報**:
- API エンドポイント: POST /api/auth/login, POST /api/auth/register
- React Sample 参考: features/auth/api/login.ts
- セキュリティ: bcrypt パスワードハッシュ、rate limiting

---

## UC004: 🖥️求人作成🌊

**使用時のコンテキスト**: 採用担当者が新しい求人を作成する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 採用担当者

**利害関係者と利益**:
- **採用担当者**: 効率的な求人情報登録
- **求職者**: 新しい求人機会へのアクセス

**事前条件**:
- ユーザーが採用担当者権限でログイン済み
- 所属組織が有効状態

**最低保証**: 求人作成の成功/失敗が適切に応答される

**成功保証時**: 新規求人がデータベースに保存され、求人IDが返される

**トリガー**: POST /api/jobs API呼び出し

**主成功シナリオ**:
1. システムが求人作成データを受信
2. システムがJWTトークンから権限を検証
3. システムが入力データをバリデーション（必須項目、制約）
4. システムが求人オブジェクトを構築
5. システムがデータベースに新規求人を保存
6. システムが検索インデックスを更新
7. システムがHTTP 201とともに作成された求人情報を返却

**拡張**:
- 2a 認証エラー: HTTP 401 Unauthorized を返却
- 2b 権限エラー: HTTP 403 Forbidden を返却
- 3a バリデーションエラー: HTTP 400 Bad Request を返却
- 5a データベースエラー: HTTP 500 Internal Server Error を返却

**技術およびデータのバリエーションリスト**:
- 入力データ: CreateJobData (position, department, location, info)
- バリデーション: 必須項目、文字数制限、HTML サニタイズ
- レスポンス: 作成された Job オブジェクト

**関連情報**:
- API エンドポイント: POST /api/jobs
- React Sample 参考: features/jobs/api/create-job.ts
- 承認フロー: 組織ポリシーに応じた管理者承認

---

## UC005: 🖥️求人更新🌊

**使用時のコンテキスト**: 採用担当者が既存求人情報を更新する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 採用担当者

**利害関係者と利益**:
- **採用担当者**: 柔軟な求人情報管理
- **求職者**: 最新の正確な求人情報

**事前条件**:
- 更新対象の求人が存在
- ユーザーが該当求人の編集権限を保持

**最低保証**: 更新の成功/失敗が適切に応答される

**成功保証時**: 求人情報が正常に更新され、変更が反映される

**トリガー**: PUT /api/jobs/{jobId} API呼び出し

**主成功シナリオ**:
1. システムが求人更新データと求人IDを受信
2. システムがJWTトークンから権限を検証
3. システムが既存求人の存在と編集権限を確認
4. システムが更新データをバリデーション
5. システムが求人情報を更新
6. システムが検索インデックスを更新
7. システムがHTTP 200とともに更新された求人情報を返却

**拡張**:
- 2a 認証エラー: HTTP 401 Unauthorized を返却
- 3a 求人が存在しない: HTTP 404 Not Found を返却
- 3b 編集権限なし: HTTP 403 Forbidden を返却
- 4a バリデーションエラー: HTTP 400 Bad Request を返却
- 5a 楽観的ロックエラー: HTTP 409 Conflict を返却

**技術およびデータのバリエーションリスト**:
- 更新データ: 部分更新対応（PATCH セマンティクス）
- 競合制御: version フィールドによる楽観的ロック
- 履歴管理: 変更履歴の記録とロールバック機能

**関連情報**:
- API エンドポイント: PUT /api/jobs/:id
- 権限管理: 組織内の求人編集権限チェック
- 通知システム: 重要変更時の関係者通知

---

## UC006: 🖥️求人削除🌊

**使用時のコンテキスト**: 採用担当者が不要な求人を削除する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 採用担当者

**利害関係者と利益**:
- **採用担当者**: 不要求人の適切な管理
- **システム**: データ整合性の維持

**事前条件**:
- 削除対象の求人が存在
- ユーザーが該当求人の削除権限を保持

**最低保証**: 削除の成功/失敗が適切に応答される

**成功保証時**: 求人が論理削除され、検索対象から除外される

**トリガー**: DELETE /api/jobs/{jobId} API呼び出し

**主成功シナリオ**:
1. システムが削除対象の求人IDを受信
2. システムがJWTトークンから権限を検証
3. システムが求人の存在と削除権限を確認
4. システムが関連する応募データの存在を確認
5. システムが求人を論理削除状態に更新
6. システムが検索インデックスから削除
7. システムがHTTP 204 No Content を返却

**拡張**:
- 2a 認証エラー: HTTP 401 Unauthorized を返却
- 3a 求人が存在しない: HTTP 404 Not Found を返却
- 3b 削除権限なし: HTTP 403 Forbidden を返却
- 4a 応募データ存在: 警告とともに論理削除実行
- 5a データベースエラー: HTTP 500 Internal Server Error を返却

**技術およびデータのバリエーションリスト**:
- 削除方式: 論理削除（isDeleted フラグ）
- 関連データ: 応募情報は保持、求人情報のみ非表示
- 復元機能: 管理者による削除求人の復元対応

**関連情報**:
- API エンドポイント: DELETE /api/jobs/:id
- データ保護: GDPR対応の個人データ処理
- 監査ログ: 削除操作の完全な記録

---

## UC007: 🖥️応募申請🌊

**使用時のコンテキスト**: 求職者が興味のある求人に応募する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 求職者

**利害関係者と利益**:
- **求職者**: 効率的な求人応募プロセス
- **採用企業**: 応募者情報の体系的な収集

**事前条件**:
- 応募対象の求人が公開状態
- 求人が応募受付中

**最低保証**: 応募の成功/失敗が適切に応答される

**成功保証時**: 応募情報がデータベースに保存され、通知が送信される

**トリガー**: POST /api/jobs/{jobId}/applications API呼び出し

**主成功シナリオ**:
1. システムが応募データと求人IDを受信
2. システムが求人の存在と応募可能状態を確認
3. システムが応募データをバリデーション
4. システムが重複応募をチェック
5. システムが応募情報をデータベースに保存
6. システムが応募者に確認メールを送信
7. システムが採用担当者に通知を送信
8. システムがHTTP 201とともに応募IDを返却

**拡張**:
- 2a 求人が存在しない: HTTP 404 Not Found を返却
- 2b 求人が応募終了: HTTP 400 Bad Request を返却
- 3a バリデーションエラー: HTTP 400 Bad Request を返却
- 4a 重複応募: HTTP 409 Conflict を返却
- 6a メール送信失敗: 応募は正常処理、ログに記録
- 7a 通知送信失敗: 応募は正常処理、再試行キューに追加

**技術およびデータのバリエーションリスト**:
- 応募データ: 基本情報、履歴書ファイル、志望動機
- ファイル処理: マルチパートアップロード、ウイルススキャン
- 通知システム: メール、プッシュ通知、システム内通知

**関連情報**:
- API エンドポイント: POST /api/jobs/:id/applications
- ファイル制限: 最大5MB、PDF/DOC形式
- プライバシー: 個人情報保護法対応

---

## UC008: 🖥️応募一覧取得🌊

**使用時のコンテキスト**: 採用担当者が管理する求人の応募一覧を確認する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 採用担当者

**利害関係者と利益**:
- **採用担当者**: 効率的な応募者管理
- **組織管理者**: 採用活動の監督

**事前条件**:
- ユーザーが採用担当者権限でログイン済み
- 指定求人への管理権限を保持

**最低保証**: 応募一覧または適切なエラーレスポンス

**成功保証時**: 権限のある求人の応募一覧が返される

**トリガー**: GET /api/jobs/{jobId}/applications API呼び出し

**主成功シナリオ**:
1. システムが求人IDとクエリパラメータを受信
2. システムがJWTトークンから権限を検証
3. システムが求人への管理権限を確認
4. システムがフィルター条件を適用
5. システムが応募一覧をデータベースから取得
6. システムが個人情報をマスキング（権限に応じて）
7. システムがHTTP 200とともに応募一覧を返却

**拡張**:
- 2a 認証エラー: HTTP 401 Unauthorized を返却
- 3a 管理権限なし: HTTP 403 Forbidden を返却
- 4a 無効なフィルター: HTTP 400 Bad Request を返却
- 6a 権限不足: 基本情報のみ表示

**技術およびデータのバリエーションリスト**:
- フィルター: 応募日、選考状況、評価
- ソート: 応募日順、評価順、名前順
- ページネーション: 20件/ページのデフォルト

**関連情報**:
- API エンドポイント: GET /api/jobs/:id/applications
- 権限管理: ロールベースアクセス制御
- データ保護: 個人情報の適切なマスキング

---

## UC009: 🖥️応募状況更新🌊

**使用時のコンテキスト**: 採用担当者が応募者の選考状況を更新する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 採用担当者

**利害関係者と利益**:
- **採用担当者**: 効率的な選考プロセス管理
- **応募者**: 選考状況の透明な管理

**事前条件**:
- 対象応募が存在
- ユーザーが該当求人の管理権限を保持

**最低保証**: 更新の成功/失敗が適切に応答される

**成功保証時**: 応募状況が更新され、必要に応じて通知が送信される

**トリガー**: PUT /api/applications/{applicationId} API呼び出し

**主成功シナリオ**:
1. システムが応募IDと更新データを受信
2. システムがJWTトークンから権限を検証
3. システムが応募の存在と管理権限を確認
4. システムが更新データをバリデーション
5. システムが応募状況を更新
6. システムが状況変更履歴を記録
7. システムが応募者に状況変更通知を送信（設定による）
8. システムがHTTP 200とともに更新結果を返却

**拡張**:
- 2a 認証エラー: HTTP 401 Unauthorized を返却
- 3a 応募が存在しない: HTTP 404 Not Found を返却
- 3b 管理権限なし: HTTP 403 Forbidden を返却
- 4a 無効な状況遷移: HTTP 400 Bad Request を返却
- 7a 通知送信失敗: 更新は正常処理、ログに記録

**技術およびデータのバリエーションリスト**:
- 選考状況: 新規応募、書類選考中、面接調整中、面接実施中、最終選考中、内定、不採用、辞退
- 状況遷移: ビジネスルールに基づく有効な遷移のみ許可
- 履歴管理: 全ての状況変更を監査ログに記録

**関連情報**:
- API エンドポイント: PUT /api/applications/:id
- 通知設定: 応募者への自動/手動通知制御
- ワークフロー: 複数段階の承認プロセス対応

---

## UC010: 🖥️組織管理🌊

**使用時のコンテキスト**: 組織管理者が組織情報や所属ユーザーを管理する

**スコープ**: React Job Board Application システム

**レベル**: ユーザー目的

**主アクター**: 組織管理者

**利害関係者と利益**:
- **組織管理者**: 統合的な組織運営管理
- **採用担当者**: 適切な権限と環境での作業

**事前条件**:
- ユーザーが組織管理者権限でログイン済み
- 対象組織への管理権限を保持

**最低保証**: 組織情報または適切なエラーレスポンス

**成功保証時**: 組織情報と関連データが適切に管理される

**トリガー**: GET/PUT /api/organizations/{organizationId} API呼び出し

**主成功シナリオ**:
1. システムが組織IDまたは更新データを受信
2. システムがJWTトークンから権限を検証
3. システムが組織への管理権限を確認
4. システムが要求されたアクション（取得/更新）を実行
5. システムが結果をレスポンス

**拡張**:
- 2a 認証エラー: HTTP 401 Unauthorized を返却
- 3a 管理権限なし: HTTP 403 Forbidden を返却
- 4a 更新時バリデーションエラー: HTTP 400 Bad Request を返却

**技術およびデータのバリエーションリスト**:
- 組織情報: 基本情報、設定、ユーザー一覧
- 権限管理: 階層的な権限構造
- 監査機能: 組織変更の完全な記録

**関連情報**:
- API エンドポイント: GET/PUT /api/organizations/:id
- React Sample 参考: features/organizations
- 権限階層: 組織管理者 > 採用担当者 > 一般ユーザー

---

## システムアーキテクチャとの対応

### API エンドポイント マッピング

| システムユースケース | HTTP メソッド | エンドポイント | React Sample 参考 |
|-------------------|-------------|---------------|------------------|
| UC001: 求人検索実行 | GET | /api/jobs | features/jobs/api/get-jobs.ts |
| UC002: 求人詳細取得 | GET | /api/jobs/:id | features/jobs/api/get-job.ts |
| UC003: ユーザー認証 | POST | /api/auth/login | features/auth/api/login.ts |
| UC004: 求人作成 | POST | /api/jobs | features/jobs/api/create-job.ts |
| UC005: 求人更新 | PUT | /api/jobs/:id | features/jobs/api/update-job.ts |
| UC006: 求人削除 | DELETE | /api/jobs/:id | features/jobs/api/delete-job.ts |
| UC007: 応募申請 | POST | /api/jobs/:id/applications | 新規実装 |
| UC008: 応募一覧取得 | GET | /api/jobs/:id/applications | 新規実装 |
| UC009: 応募状況更新 | PUT | /api/applications/:id | 新規実装 |
| UC010: 組織管理 | GET/PUT | /api/organizations/:id | features/organizations |

### 非機能要件

#### パフォーマンス
- API レスポンス時間: 平均 < 1秒、95パーセンタイル < 3秒
- 同時接続数: 最大1000ユーザー
- ファイルアップロード: 最大5MB/リクエスト

#### セキュリティ
- 認証: JWT (24時間有効期限)
- 認可: ロールベースアクセス制御 (RBAC)
- データ保護: HTTPS通信、個人情報暗号化

#### 可用性
- システム稼働率: 99.9% (月間 8.76時間以内のダウンタイム)
- バックアップ: 日次増分、週次フル
- 障害復旧: RTO 4時間、RPO 1時間

---

## 次のステップ

1. **ユーザーストーリーの作成**: システムユースケースを実装可能な単位に分解
2. **API仕様書の詳細化**: OpenAPI/Swagger による正確な API 定義
3. **非機能要件の具体化**: パフォーマンス、セキュリティ、可用性の詳細要件
4. **テストケースの作成**: システムユースケースベースの受入テスト定義