# 作業履歴 2025-07-08

## 概要

2025-07-08の作業内容をまとめています。

## コミット: 0ee2856

### メッセージ

```
chore: correct typo in .gitignore from 'targetf' to 'target'
```

### 変更されたファイル

- M	.gitignore

### 変更内容

```diff
commit 0ee28560c8932b41f5b2751a213f009f40d3b09f
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Jul 8 12:15:02 2025 +0900

    chore: correct typo in .gitignore from 'targetf' to 'target'

diff --git a/.gitignore b/.gitignore
index d8b510e..6d157fe 100644
--- a/.gitignore
+++ b/.gitignore
@@ -5,5 +5,5 @@ coverage
 .cpcache
 build
 .gradle
-targetf
+target
 .env
\ No newline at end of file

```

## コミット: ba0b48a

### メッセージ

```
chore: update Docker image version to 0.0.8 in docker-compose.yml
```

### 変更されたファイル

- M	docker-compose.yml

### 変更内容

```diff
commit ba0b48a36144f77faa1fff647c526445fb4c6825
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Jul 8 11:16:50 2025 +0900

    chore: update Docker image version to 0.0.8 in docker-compose.yml

diff --git a/docker-compose.yml b/docker-compose.yml
index ba9244d..9eded97 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -1,6 +1,6 @@
 services:
   app:
-    image: ghcr.io/k2works/ai-programing-exercise/core:0.0.7
+    image: ghcr.io/k2works/ai-programing-exercise/core:0.0.8
     volumes:
       - ./:/srv/app
       - ~/.ssh/id_rsa:/root/.ssh/id_rsa

```

## コミット: 498f240

### メッセージ

```
chore: update .gitignore and Docker configurations to enhance environment setup
```

### 変更されたファイル

- M	.gitignore
- M	Dockerfile
- M	docker-compose.yml

### 変更内容

```diff
commit 498f24059d23fbcadc172b781f550bcf0b8b0be4
Author: k2works <kakimomokuri@gmail.com>
Date:   Tue Jul 8 10:10:53 2025 +0900

    chore: update .gitignore and Docker configurations to enhance environment setup

diff --git a/.gitignore b/.gitignore
index 4dd4f2c..d8b510e 100644
--- a/.gitignore
+++ b/.gitignore
@@ -5,4 +5,5 @@ coverage
 .cpcache
 build
 .gradle
-target
\ No newline at end of file
+targetf
+.env
\ No newline at end of file
diff --git a/Dockerfile b/Dockerfile
index 2a8c178..e78e828 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -36,6 +36,7 @@ RUN apt-get update && apt-get install -y \
             curl \
             wget \
             vim \
+            tmux \
             && apt-get clean \
             && rm -rf /var/lib/apt/lists/*
 
@@ -159,6 +160,12 @@ RUN apt-get update && apt-get install -y \
     && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain $RUST_VER \
     && echo 'source $HOME/.cargo/env' >> ~/.bashrc
 
+# Gemini CLIのインストール
+RUN npm install -g @google/gemini-cli
+
+# Claude Codeのインストール
+RUN npm install -g @anthropic-ai/claude-code
+
 # パスの設定
 ENV PATH="/root/.cargo/bin:/usr/local/go/bin:/root/.ghcup/bin:/root/.sdkman/candidates/java/current/bin:/root/.sdkman/candidates/maven/current/bin:/root/.sdkman/candidates/gradle/current/bin:/root/.rbenv/shims:/usr/local/bin:$PATH"
 
diff --git a/docker-compose.yml b/docker-compose.yml
index 6def9b6..ba9244d 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -2,7 +2,9 @@ services:
   app:
     image: ghcr.io/k2works/ai-programing-exercise/core:0.0.7
     volumes:
-      - ./:/srv
+      - ./:/srv/app
+      - ~/.ssh/id_rsa:/root/.ssh/id_rsa
+      - ~/.ssh/id_rsa.pub:/root/.ssh/id_rsa.pub
     command: bash
     tty: true
     stdin_open: true

```

