# 作業履歴 2025-07-10

## 概要

2025-07-10の作業内容をまとめています。

## コミット: 3a18137

### メッセージ

```
refactor: モジュール分割
```

### 変更されたファイル

- A	app/application/fizz_buzz_command.go
- A	app/application/fizz_buzz_list_command.go
- A	app/application/fizz_buzz_value_command.go
- A	app/domain/model/fizz_buzz_list.go
- A	app/domain/model/fizz_buzz_value.go
- A	app/domain/type/fizz_buzz_type.go
- A	app/domain/type/fizz_buzz_type_01.go
- A	app/domain/type/fizz_buzz_type_02.go
- A	app/domain/type/fizz_buzz_type_03.go
- A	app/domain/type/fizz_buzz_type_not_defined.go
- A	app/fizzbuzz
- M	app/fizzbuzz.go
- M	app/go.mod
- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 3a181375af16bd9c37a8a7be902902d48571ec05
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 08:39:51 2025 +0000

    refactor: モジュール分割

diff --git a/app/application/fizz_buzz_command.go b/app/application/fizz_buzz_command.go
new file mode 100644
index 0000000..523819b
--- /dev/null
+++ b/app/application/fizz_buzz_command.go
@@ -0,0 +1,6 @@
+package application
+
+// FizzBuzzCommand Commandパターンのインターフェース
+type FizzBuzzCommand interface {
+	Execute(number int) interface{}
+}
diff --git a/app/application/fizz_buzz_list_command.go b/app/application/fizz_buzz_list_command.go
new file mode 100644
index 0000000..6c0cfca
--- /dev/null
+++ b/app/application/fizz_buzz_list_command.go
@@ -0,0 +1,32 @@
+package application
+
+import (
+	"fmt"
+	"fizzbuzz/domain/model"
+	"fizzbuzz/domain/type"
+)
+
+// FizzBuzzListCommand ファーストクラスコレクションを返すコマンド
+type FizzBuzzListCommand struct {
+	typeImpl fizzbuzztype.FizzBuzzType
+}
+
+// NewFizzBuzzListCommand コンストラクタ
+func NewFizzBuzzListCommand(typeImpl fizzbuzztype.FizzBuzzType) *FizzBuzzListCommand {
+	return &FizzBuzzListCommand{
+		typeImpl: typeImpl,
+	}
+}
+
+// Execute 指定した数までのFizzBuzzリストを生成して返す
+func (c *FizzBuzzListCommand) Execute(number int) interface{} {
+	const MaxCount = 100
+	if number > MaxCount {
+		panic(model.NewInvalidValueError(fmt.Sprintf("%d より多い数を許可しない", MaxCount)))
+	}
+	values := make([]model.FizzBuzzValue, 0, number)
+	for i := 1; i <= number; i++ {
+		values = append(values, c.typeImpl.Generate(i))
+	}
+	return model.NewFizzBuzzList(values).Value()
+}
diff --git a/app/application/fizz_buzz_value_command.go b/app/application/fizz_buzz_value_command.go
new file mode 100644
index 0000000..8ef6cfa
--- /dev/null
+++ b/app/application/fizz_buzz_value_command.go
@@ -0,0 +1,20 @@
+package application
+
+import "fizzbuzz/domain/type"
+
+// FizzBuzzValueCommand 値オブジェクトを返すコマンド
+type FizzBuzzValueCommand struct {
+	typeImpl fizzbuzztype.FizzBuzzType
+}
+
+// NewFizzBuzzValueCommand コンストラクタ
+func NewFizzBuzzValueCommand(typeImpl fizzbuzztype.FizzBuzzType) *FizzBuzzValueCommand {
+	return &FizzBuzzValueCommand{
+		typeImpl: typeImpl,
+	}
+}
+
+// Execute 値オブジェクトの値部分を返す
+func (c *FizzBuzzValueCommand) Execute(number int) interface{} {
+	return c.typeImpl.Generate(number).Value()
+}
diff --git a/app/domain/model/fizz_buzz_list.go b/app/domain/model/fizz_buzz_list.go
new file mode 100644
index 0000000..62ece3c
--- /dev/null
+++ b/app/domain/model/fizz_buzz_list.go
@@ -0,0 +1,52 @@
+package model
+
+import "fmt"
+
+// FizzBuzzList ファーストクラスコレクション
+type FizzBuzzList struct {
+	value []FizzBuzzValue
+}
+
+// MAX_COUNT 最大件数
+const MAX_COUNT = 100
+
+// NewFizzBuzzList コンストラクタ
+func NewFizzBuzzList(values []FizzBuzzValue) *FizzBuzzList {
+	if len(values) > MAX_COUNT {
+		panic(NewInvalidValueError(fmt.Sprintf("上限は%d件までです", MAX_COUNT)))
+	}
+	// イミュータブルにするため新しいスライスを作成
+	newValues := make([]FizzBuzzValue, len(values))
+	copy(newValues, values)
+	return &FizzBuzzList{
+		value: newValues,
+	}
+}
+
+// Value 値を取得（読み取り専用）
+func (f *FizzBuzzList) Value() []FizzBuzzValue {
+	// 外部からの変更を防ぐためコピーを返す
+	result := make([]FizzBuzzValue, len(f.value))
+	copy(result, f.value)
+	return result
+}
+
+// String 文字列表現
+func (f *FizzBuzzList) String() string {
+	return fmt.Sprintf("%v", f.value)
+}
+
+// Add 新しい要素を追加した新しいFizzBuzzListを返す（イミュータブル）
+func (f *FizzBuzzList) Add(values []FizzBuzzValue) *FizzBuzzList {
+	newValues := make([]FizzBuzzValue, len(f.value)+len(values))
+	copy(newValues, f.value)
+	copy(newValues[len(f.value):], values)
+	return &FizzBuzzList{
+		value: newValues,
+	}
+}
+
+// Count 要素数を取得
+func (f *FizzBuzzList) Count() int {
+	return len(f.value)
+}
diff --git a/app/domain/model/fizz_buzz_value.go b/app/domain/model/fizz_buzz_value.go
new file mode 100644
index 0000000..7dbad78
--- /dev/null
+++ b/app/domain/model/fizz_buzz_value.go
@@ -0,0 +1,54 @@
+package model
+
+import "fmt"
+
+// InvalidValueError 不正な値エラー
+type InvalidValueError struct {
+	message string
+}
+
+func (e InvalidValueError) Error() string {
+	return e.message
+}
+
+// NewInvalidValueError コンストラクタ
+func NewInvalidValueError(message string) InvalidValueError {
+	return InvalidValueError{message: message}
+}
+
+// FizzBuzzValue 値オブジェクト
+type FizzBuzzValue struct {
+	number int
+	value  string
+}
+
+// NewFizzBuzzValue コンストラクタ
+func NewFizzBuzzValue(number int, value string) FizzBuzzValue {
+	if number < 0 {
+		panic(NewInvalidValueError("値は正の値のみ許可"))
+	}
+	return FizzBuzzValue{
+		number: number,
+		value:  value,
+	}
+}
+
+// Number 数値を取得
+func (f FizzBuzzValue) Number() int {
+	return f.number
+}
+
+// Value 値を取得
+func (f FizzBuzzValue) Value() string {
+	return f.value
+}
+
+// String 文字列表現
+func (f FizzBuzzValue) String() string {
+	return fmt.Sprintf("%d:%s", f.number, f.value)
+}
+
+// Equal 等価性チェック
+func (f FizzBuzzValue) Equal(other FizzBuzzValue) bool {
+	return f.number == other.number && f.value == other.value
+}
diff --git a/app/domain/type/fizz_buzz_type.go b/app/domain/type/fizz_buzz_type.go
new file mode 100644
index 0000000..74bfed1
--- /dev/null
+++ b/app/domain/type/fizz_buzz_type.go
@@ -0,0 +1,40 @@
+package fizzbuzztype
+
+import "fizzbuzz/domain/model"
+
+// FizzBuzzType インターフェース
+type FizzBuzzType interface {
+	Generate(number int) model.FizzBuzzValue
+}
+
+// FizzBuzzTypeBase 共通処理を提供する基底構造体
+type FizzBuzzTypeBase struct{}
+
+// タイプ定数
+const (
+	TYPE_01 = 1
+	TYPE_02 = 2
+	TYPE_03 = 3
+)
+
+// Create ファクトリメソッド
+func (f FizzBuzzTypeBase) Create(fizzBuzzType int) FizzBuzzType {
+	switch fizzBuzzType {
+	case TYPE_01:
+		return FizzBuzzType01{}
+	case TYPE_02:
+		return FizzBuzzType02{}
+	case TYPE_03:
+		return FizzBuzzType03{}
+	default:
+		return FizzBuzzTypeNotDefined{}
+	}
+}
+
+func (f FizzBuzzTypeBase) Fizz(number int) bool {
+	return number%3 == 0
+}
+
+func (f FizzBuzzTypeBase) Buzz(number int) bool {
+	return number%5 == 0
+}
diff --git a/app/domain/type/fizz_buzz_type_01.go b/app/domain/type/fizz_buzz_type_01.go
new file mode 100644
index 0000000..7f9a440
--- /dev/null
+++ b/app/domain/type/fizz_buzz_type_01.go
@@ -0,0 +1,27 @@
+package fizzbuzztype
+
+import (
+	"strconv"
+	"fizzbuzz/domain/model"
+)
+
+// FizzBuzzType01 タイプ1の実装
+type FizzBuzzType01 struct {
+	FizzBuzzTypeBase
+}
+
+func (f FizzBuzzType01) Generate(number int) model.FizzBuzzValue {
+	isFizz := f.Fizz(number)
+	isBuzz := f.Buzz(number)
+
+	if isFizz && isBuzz {
+		return model.NewFizzBuzzValue(number, "FizzBuzz")
+	}
+	if isFizz {
+		return model.NewFizzBuzzValue(number, "Fizz")
+	}
+	if isBuzz {
+		return model.NewFizzBuzzValue(number, "Buzz")
+	}
+	return model.NewFizzBuzzValue(number, strconv.Itoa(number))
+}
diff --git a/app/domain/type/fizz_buzz_type_02.go b/app/domain/type/fizz_buzz_type_02.go
new file mode 100644
index 0000000..3f688df
--- /dev/null
+++ b/app/domain/type/fizz_buzz_type_02.go
@@ -0,0 +1,15 @@
+package fizzbuzztype
+
+import (
+	"strconv"
+	"fizzbuzz/domain/model"
+)
+
+// FizzBuzzType02 タイプ2の実装
+type FizzBuzzType02 struct {
+	FizzBuzzTypeBase
+}
+
+func (f FizzBuzzType02) Generate(number int) model.FizzBuzzValue {
+	return model.NewFizzBuzzValue(number, strconv.Itoa(number))
+}
diff --git a/app/domain/type/fizz_buzz_type_03.go b/app/domain/type/fizz_buzz_type_03.go
new file mode 100644
index 0000000..3422e71
--- /dev/null
+++ b/app/domain/type/fizz_buzz_type_03.go
@@ -0,0 +1,21 @@
+package fizzbuzztype
+
+import (
+	"strconv"
+	"fizzbuzz/domain/model"
+)
+
+// FizzBuzzType03 タイプ3の実装
+type FizzBuzzType03 struct {
+	FizzBuzzTypeBase
+}
+
+func (f FizzBuzzType03) Generate(number int) model.FizzBuzzValue {
+	isFizz := f.Fizz(number)
+	isBuzz := f.Buzz(number)
+
+	if isFizz && isBuzz {
+		return model.NewFizzBuzzValue(number, "FizzBuzz")
+	}
+	return model.NewFizzBuzzValue(number, strconv.Itoa(number))
+}
diff --git a/app/domain/type/fizz_buzz_type_not_defined.go b/app/domain/type/fizz_buzz_type_not_defined.go
new file mode 100644
index 0000000..6bb4fcc
--- /dev/null
+++ b/app/domain/type/fizz_buzz_type_not_defined.go
@@ -0,0 +1,16 @@
+package fizzbuzztype
+
+import "fizzbuzz/domain/model"
+
+// FizzBuzzTypeNotDefined 未定義タイプの実装（Null Objectパターン）
+type FizzBuzzTypeNotDefined struct {
+	FizzBuzzTypeBase
+}
+
+func (f FizzBuzzTypeNotDefined) Generate(number int) model.FizzBuzzValue {
+	return model.NewFizzBuzzValue(number, "")
+}
+
+func (f FizzBuzzTypeNotDefined) String() string {
+	return "未定義"
+}
diff --git a/app/fizzbuzz b/app/fizzbuzz
new file mode 100644
index 0000000..9594dfe
Binary files /dev/null and b/app/fizzbuzz differ
diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index e32c1bf..4e3f4f8 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -1,207 +1,62 @@
 package main
 
 import (
-	"fmt"
 	"strconv"
+	"fizzbuzz/domain/model"
+	"fizzbuzz/domain/type"
 )
 
-// InvalidValueError 不正な値エラー
-type InvalidValueError struct {
-	message string
-}
-
-func (e InvalidValueError) Error() string {
-	return e.message
-}
-
-// NewInvalidValueError コンストラクタ
-func NewInvalidValueError(message string) InvalidValueError {
-	return InvalidValueError{message: message}
-}
-
-// FizzBuzzValue 値オブジェクト
-type FizzBuzzValue struct {
-	number int
-	value  string
-}
-
-// NewFizzBuzzValue コンストラクタ
-func NewFizzBuzzValue(number int, value string) FizzBuzzValue {
-	if number < 0 {
-		panic(NewInvalidValueError("値は正の値のみ許可"))
-	}
-	return FizzBuzzValue{
-		number: number,
-		value:  value,
-	}
-}
-
-// Number 数値を取得
-func (f FizzBuzzValue) Number() int {
-	return f.number
-}
-
-// Value 値を取得
-func (f FizzBuzzValue) Value() string {
-	return f.value
-}
-
-// String 文字列表現
-func (f FizzBuzzValue) String() string {
-	return fmt.Sprintf("%d:%s", f.number, f.value)
-}
-
-// Equal 等価性チェック
-func (f FizzBuzzValue) Equal(other FizzBuzzValue) bool {
-	return f.number == other.number && f.value == other.value
-}
-
-// FizzBuzzType インターフェース
-type FizzBuzzType interface {
-	Generate(number int) FizzBuzzValue
-}
-
-// FizzBuzzTypeBase 共通処理を提供する基底構造体
-type FizzBuzzTypeBase struct{}
-
-// タイプ定数
-const (
-	TYPE_01 = 1
-	TYPE_02 = 2
-	TYPE_03 = 3
-)
-
-// Create ファクトリメソッド
-func (f FizzBuzzTypeBase) Create(fizzBuzzType int) FizzBuzzType {
-	switch fizzBuzzType {
-	case TYPE_01:
-		return FizzBuzzType01{}
-	case TYPE_02:
-		return FizzBuzzType02{}
-	case TYPE_03:
-		return FizzBuzzType03{}
-	default:
-		return FizzBuzzTypeNotDefined{}
-	}
-}
-
-func (f FizzBuzzTypeBase) Fizz(number int) bool {
-	return number%3 == 0
-}
-
-func (f FizzBuzzTypeBase) Buzz(number int) bool {
-	return number%5 == 0
-}
-
-// FizzBuzzType01 タイプ1の実装
-type FizzBuzzType01 struct {
-	FizzBuzzTypeBase
-}
-
-func (f FizzBuzzType01) Generate(number int) FizzBuzzValue {
-	isFizz := f.Fizz(number)
-	isBuzz := f.Buzz(number)
-
-	if isFizz && isBuzz {
-		return NewFizzBuzzValue(number, "FizzBuzz")
-	}
-	if isFizz {
-		return NewFizzBuzzValue(number, "Fizz")
-	}
-	if isBuzz {
-		return NewFizzBuzzValue(number, "Buzz")
-	}
-	return NewFizzBuzzValue(number, strconv.Itoa(number))
-}
-
-// FizzBuzzType02 タイプ2の実装
-type FizzBuzzType02 struct {
-	FizzBuzzTypeBase
-}
-
-func (f FizzBuzzType02) Generate(number int) FizzBuzzValue {
-	return NewFizzBuzzValue(number, strconv.Itoa(number))
-}
-
-// FizzBuzzType03 タイプ3の実装
-type FizzBuzzType03 struct {
-	FizzBuzzTypeBase
-}
-
-func (f FizzBuzzType03) Generate(number int) FizzBuzzValue {
-	isFizz := f.Fizz(number)
-	isBuzz := f.Buzz(number)
-
-	if isFizz && isBuzz {
-		return NewFizzBuzzValue(number, "FizzBuzz")
-	}
-	return NewFizzBuzzValue(number, strconv.Itoa(number))
-}
-
-// FizzBuzzTypeNotDefined 未定義タイプの実装（Null Objectパターン）
-type FizzBuzzTypeNotDefined struct {
-	FizzBuzzTypeBase
-}
-
-func (f FizzBuzzTypeNotDefined) Generate(number int) FizzBuzzValue {
-	return NewFizzBuzzValue(number, "")
-}
-
-func (f FizzBuzzTypeNotDefined) String() string {
-	return "未定義"
-}
-
 // FizzBuzz構造体
 type FizzBuzz struct {
-	list         *FizzBuzzList // FizzBuzzListを保持するフィールド
+	list         *model.FizzBuzzList // FizzBuzzListを保持するフィールド
 	fizzBuzzType int           // FizzBuzzのタイプを保持するフィールド
-	typeImpl     FizzBuzzType  // タイプ実装
+	typeImpl     fizzbuzztype.FizzBuzzType  // タイプ実装
 }
 
 // NewFizzBuzz コンストラクタ（プリミティブ型を受け取る）
 func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
-	base := FizzBuzzTypeBase{}
+	base := fizzbuzztype.FizzBuzzTypeBase{}
 	typeImpl := base.Create(fizzBuzzType)
 
 	// 未定義タイプの場合は-1を設定
 	actualType := fizzBuzzType
-	if _, ok := typeImpl.(FizzBuzzTypeNotDefined); ok {
+	if _, ok := typeImpl.(fizzbuzztype.FizzBuzzTypeNotDefined); ok {
 		actualType = -1
 	}
 
 	return &FizzBuzz{
-		list:         NewFizzBuzzList([]FizzBuzzValue{}),
+		list:         model.NewFizzBuzzList([]model.FizzBuzzValue{}),
 		fizzBuzzType: actualType,
 		typeImpl:     typeImpl,
 	}
 }
 
 // NewFizzBuzzWithType コンストラクタ（値オブジェクトを受け取る）
-func NewFizzBuzzWithType(typeImpl FizzBuzzType) *FizzBuzz {
+func NewFizzBuzzWithType(typeImpl fizzbuzztype.FizzBuzzType) *FizzBuzz {
 	// タイプを逆算（実際の実装では避けるべきですが、ここでは後方互換性のため）
 	var fizzBuzzType int
 	switch typeImpl.(type) {
-	case FizzBuzzType01:
-		fizzBuzzType = TYPE_01
-	case FizzBuzzType02:
-		fizzBuzzType = TYPE_02
-	case FizzBuzzType03:
-		fizzBuzzType = TYPE_03
-	case FizzBuzzTypeNotDefined:
+	case fizzbuzztype.FizzBuzzType01:
+		fizzBuzzType = fizzbuzztype.TYPE_01
+	case fizzbuzztype.FizzBuzzType02:
+		fizzBuzzType = fizzbuzztype.TYPE_02
+	case fizzbuzztype.FizzBuzzType03:
+		fizzBuzzType = fizzbuzztype.TYPE_03
+	case fizzbuzztype.FizzBuzzTypeNotDefined:
 		fizzBuzzType = -1 // 未定義タイプを示すデフォルト値
 	default:
 		fizzBuzzType = -1 // 未定義タイプを示すデフォルト値
 	}
 
 	return &FizzBuzz{
-		list:         NewFizzBuzzList([]FizzBuzzValue{}),
+		list:         model.NewFizzBuzzList([]model.FizzBuzzValue{}),
 		fizzBuzzType: fizzBuzzType,
 		typeImpl:     typeImpl,
 	}
 }
 
 // Generate 数を文字列に変換してFizzBuzzルールを適用
-func (f *FizzBuzz) Generate(number int) FizzBuzzValue {
+func (f *FizzBuzz) Generate(number int) model.FizzBuzzValue {
 	return f.typeImpl.Generate(number)
 }
 
@@ -211,109 +66,19 @@ func (f *FizzBuzz) Type() int {
 }
 
 // List FizzBuzz配列を取得
-func (f *FizzBuzz) List() []FizzBuzzValue {
+func (f *FizzBuzz) List() []model.FizzBuzzValue {
 	return f.list.Value()
 }
 
 // GenerateList 範囲指定してFizzBuzzのリストを作成し、インスタンス変数に保存
 func (f *FizzBuzz) GenerateList(start, end int) {
-	values := make([]FizzBuzzValue, 0, end-start+1)
+	values := make([]model.FizzBuzzValue, 0, end-start+1)
 	for i := start; i <= end; i++ {
 		values = append(values, f.Generate(i))
 	}
 	f.list = f.list.Add(values)
 }
 
-// FizzBuzzList ファーストクラスコレクション
-type FizzBuzzList struct {
-	value []FizzBuzzValue
-}
-
-// NewFizzBuzzList コンストラクタ
-func NewFizzBuzzList(values []FizzBuzzValue) *FizzBuzzList {
-	// イミュータブルにするため新しいスライスを作成
-	newValues := make([]FizzBuzzValue, len(values))
-	copy(newValues, values)
-	return &FizzBuzzList{
-		value: newValues,
-	}
-}
-
-// Value 値を取得（読み取り専用）
-func (f *FizzBuzzList) Value() []FizzBuzzValue {
-	// 外部からの変更を防ぐためコピーを返す
-	result := make([]FizzBuzzValue, len(f.value))
-	copy(result, f.value)
-	return result
-}
-
-// String 文字列表現
-func (f *FizzBuzzList) String() string {
-	return fmt.Sprintf("%v", f.value)
-}
-
-// Add 新しい要素を追加した新しいFizzBuzzListを返す（イミュータブル）
-func (f *FizzBuzzList) Add(values []FizzBuzzValue) *FizzBuzzList {
-	newValues := make([]FizzBuzzValue, len(f.value)+len(values))
-	copy(newValues, f.value)
-	copy(newValues[len(f.value):], values)
-	return &FizzBuzzList{
-		value: newValues,
-	}
-}
-
-// Count 要素数を取得
-func (f *FizzBuzzList) Count() int {
-	return len(f.value)
-}
-
-// FizzBuzzCommand Commandパターンのインターフェース
-type FizzBuzzCommand interface {
-	Execute(number int) interface{}
-}
-
-// FizzBuzzValueCommand 値オブジェクトを返すコマンド
-type FizzBuzzValueCommand struct {
-	typeImpl FizzBuzzType
-}
-
-// NewFizzBuzzValueCommand コンストラクタ
-func NewFizzBuzzValueCommand(typeImpl FizzBuzzType) *FizzBuzzValueCommand {
-	return &FizzBuzzValueCommand{
-		typeImpl: typeImpl,
-	}
-}
-
-// Execute 値オブジェクトの値部分を返す
-func (c *FizzBuzzValueCommand) Execute(number int) interface{} {
-	return c.typeImpl.Generate(number).Value()
-}
-
-// FizzBuzzListCommand ファーストクラスコレクションを返すコマンド
-type FizzBuzzListCommand struct {
-	typeImpl FizzBuzzType
-}
-
-// NewFizzBuzzListCommand コンストラクタ
-func NewFizzBuzzListCommand(typeImpl FizzBuzzType) *FizzBuzzListCommand {
-	return &FizzBuzzListCommand{
-		typeImpl: typeImpl,
-	}
-}
-
-// Execute 指定した数までのFizzBuzzリストを生成して返す
-func (c *FizzBuzzListCommand) Execute(number int) interface{} {
-	const MaxCount = 100
-	if number > MaxCount {
-		panic(NewInvalidValueError(fmt.Sprintf("%d より多い数を許可しない", MaxCount)))
-	}
-	values := make([]FizzBuzzValue, 0, number)
-	for i := 1; i <= number; i++ {
-		values = append(values, c.typeImpl.Generate(i))
-	}
-	return NewFizzBuzzList(values).Value()
-}
-
 // BasicFizzBuzzGenerate 基本的なFizzBuzz生成関数（ドキュメントの初期バージョン）
 func BasicFizzBuzzGenerate(number, fizzBuzzType int) string {
 	isFizz := number%3 == 0
diff --git a/app/go.mod b/app/go.mod
index 1583b43..9a69f38 100644
--- a/app/go.mod
+++ b/app/go.mod
@@ -1,3 +1,3 @@
-module go-tdd-tutorial
+module fizzbuzz
 
 go 1.22.0
diff --git a/app/main.go b/app/main.go
index aa9d680..fc4993f 100644
--- a/app/main.go
+++ b/app/main.go
@@ -1,10 +1,13 @@
 package main
 
-import "fmt"
+import (
+	"fmt"
+	"fizzbuzz/domain/type"
+)
 
 func main() {
 	fmt.Println("FizzBuzz Game:")
-	fizzbuzz := NewFizzBuzz(1)
+	fizzbuzz := NewFizzBuzz(fizzbuzztype.TYPE_01)
 	fizzbuzz.GenerateList(1, 100)
 	results := fizzbuzz.List()
 	for _, result := range results {
diff --git a/app/main_test.go b/app/main_test.go
index 50dec2b..ca81656 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -1,12 +1,17 @@
 package main
 
-import "testing"
+import (
+	"testing"
+	"fizzbuzz/domain/model"
+	"fizzbuzz/domain/type"
+	"fizzbuzz/application"
+)
 
 func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected string) {
 	t.Helper()
-	base := FizzBuzzTypeBase{}
+	base := fizzbuzztype.FizzBuzzTypeBase{}
 	typeImpl := base.Create(fizzBuzzType)
-	command := NewFizzBuzzValueCommand(typeImpl)
+	command := application.NewFizzBuzzValueCommand(typeImpl)
 	got := command.Execute(number).(string)
 	if got != expected {
 		t.Errorf("FizzBuzzValueCommand.Execute(%d) with type %d = %v, want %v", number, fizzBuzzType, got, expected)
@@ -18,29 +23,29 @@ func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected strin
 
 // 三の倍数の場合
 func Test3を渡したら文字列Fizzを返す_タイプ1(t *testing.T) {
-	assertGenerateByType(t, 3, 1, "Fizz")
+	assertGenerateByType(t, 3, fizzbuzztype.TYPE_01, "Fizz")
 }
 
 // 五の倍数の場合
 func Test5を渡したら文字列Buzzを返す_タイプ1(t *testing.T) {
-	assertGenerateByType(t, 5, 1, "Buzz")
+	assertGenerateByType(t, 5, fizzbuzztype.TYPE_01, "Buzz")
 }
 
 // 三と五の倍数の場合
 func Test15を渡したら文字列FizzBuzzを返す_タイプ1(t *testing.T) {
-	assertGenerateByType(t, 15, 1, "FizzBuzz")
+	assertGenerateByType(t, 15, fizzbuzztype.TYPE_01, "FizzBuzz")
 }
 
 // その他の場合
 func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
-	assertGenerateByType(t, 1, 1, "1")
+	assertGenerateByType(t, 1, fizzbuzztype.TYPE_01, "1")
 }
 
 // 1から100までのFizzBuzzの配列を返す
 func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
-	typeImpl := FizzBuzzType01{}
-	command := NewFizzBuzzListCommand(typeImpl)
-	results := command.Execute(100).([]FizzBuzzValue)
+	typeImpl := fizzbuzztype.FizzBuzzType01{}
+	command := application.NewFizzBuzzListCommand(typeImpl)
+	results := command.Execute(100).([]model.FizzBuzzValue)
 
 	// いくつかの値を確認
 	if len(results) != 100 {
@@ -66,53 +71,53 @@ func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
 
 // その他の場合
 func Test1を渡したら文字列1を返す_タイプ2(t *testing.T) {
-	assertGenerateByType(t, 1, 2, "1")
+	assertGenerateByType(t, 1, fizzbuzztype.TYPE_02, "1")
 }
 
 // 3の倍数のときは数を文字列にして返す
 func Test3を渡したら文字列3を返す_タイプ2(t *testing.T) {
-	assertGenerateByType(t, 3, 2, "3")
+	assertGenerateByType(t, 3, fizzbuzztype.TYPE_02, "3")
 }
 
 // 5の倍数のときは数を文字列にして返す
 func Test5を渡したら文字列5を返す_タイプ2(t *testing.T) {
-	assertGenerateByType(t, 5, 2, "5")
+	assertGenerateByType(t, 5, fizzbuzztype.TYPE_02, "5")
 }
 
 // 3と5両方の倍数の場合には数を文字列にして返す
 func Test15を渡したら文字列15を返す_タイプ2(t *testing.T) {
-	assertGenerateByType(t, 15, 2, "15")
+	assertGenerateByType(t, 15, fizzbuzztype.TYPE_02, "15")
 }
 
 // タイプ3の場合
 
 // その他の場合
 func Test1を渡したら文字列1を返す_タイプ3(t *testing.T) {
-	assertGenerateByType(t, 1, 3, "1")
+	assertGenerateByType(t, 1, fizzbuzztype.TYPE_03, "1")
 }
 
 // 3の倍数のときは数を文字列にして返す
 func Test3を渡したら文字列3を返す_タイプ3(t *testing.T) {
-	assertGenerateByType(t, 3, 3, "3")
+	assertGenerateByType(t, 3, fizzbuzztype.TYPE_03, "3")
 }
 
 // 5の倍数のときは数を文字列にして返す
 func Test5を渡したら文字列5を返す_タイプ3(t *testing.T) {
-	assertGenerateByType(t, 5, 3, "5")
+	assertGenerateByType(t, 5, fizzbuzztype.TYPE_03, "5")
 }
 
 // 3と5両方の倍数の場合にはFizzBuzzと返す
 func Test15を渡したら文字列FizzBuzzを返す_タイプ3(t *testing.T) {
-	assertGenerateByType(t, 15, 3, "FizzBuzz")
+	assertGenerateByType(t, 15, fizzbuzztype.TYPE_03, "FizzBuzz")
 }
 
 // それ以外のタイプの場合
 func TestGenerateByType_それ以外のタイプで未定義のタイプを返す(t *testing.T) {
-	base := FizzBuzzTypeBase{}
+	base := fizzbuzztype.FizzBuzzTypeBase{}
 	typeImpl := base.Create(4)
 	
 	// String()メソッドをテスト
-	if notDefined, ok := typeImpl.(FizzBuzzTypeNotDefined); ok {
+	if notDefined, ok := typeImpl.(fizzbuzztype.FizzBuzzTypeNotDefined); ok {
 		got := notDefined.String()
 		expected := "未定義"
 		if got != expected {
@@ -139,7 +144,7 @@ func Test該当しないタイプを指定した場合未定義のタイプを
 
 // 値オブジェクトのテスト
 func Test値オブジェクトを使用したFizzBuzz_タイプ1(t *testing.T) {
-	typeInstance := FizzBuzzType01{}
+	typeInstance := fizzbuzztype.FizzBuzzType01{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
 	got := fizzbuzz.Generate(3)
 	if got.Value() != "Fizz" {
@@ -148,7 +153,7 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ1(t *testing.T) {
 }
 
 func Test値オブジェクトを使用したFizzBuzz_タイプ2(t *testing.T) {
-	typeInstance := FizzBuzzType02{}
+	typeInstance := fizzbuzztype.FizzBuzzType02{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
 	got := fizzbuzz.Generate(3)
 	if got.Value() != "3" {
@@ -157,7 +162,7 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ2(t *testing.T) {
 }
 
 func Test値オブジェクトを使用したFizzBuzz_タイプ3(t *testing.T) {
-	typeInstance := FizzBuzzType03{}
+	typeInstance := fizzbuzztype.FizzBuzzType03{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
 	got := fizzbuzz.Generate(15)
 	if got.Value() != "FizzBuzz" {
@@ -167,7 +172,7 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ3(t *testing.T) {
 
 // FizzBuzzListの学習用テスト
 func TestFizzBuzzList新しいインスタンスが作られる(t *testing.T) {
-	typeInstance := FizzBuzzType01{}
+	typeInstance := fizzbuzztype.FizzBuzzType01{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
 	fizzbuzz.GenerateList(1, 50)
 	
@@ -188,7 +193,7 @@ func TestFizzBuzzList新しいインスタンスが作られる(t *testing.T) {
 func Test値は正の値のみ許可する_FizzBuzzValueCommand(t *testing.T) {
 	defer func() {
 		if r := recover(); r != nil {
-			if err, ok := r.(InvalidValueError); ok {
+			if err, ok := r.(model.InvalidValueError); ok {
 				expected := "値は正の値のみ許可"
 				if err.Error() != expected {
 					t.Errorf("Expected panic message %v, got %v", expected, err.Error())
@@ -201,9 +206,9 @@ func Test値は正の値のみ許可する_FizzBuzzValueCommand(t *testing.T) {
 		}
 	}()
 	
-	base := FizzBuzzTypeBase{}
-	typeImpl := base.Create(TYPE_01)
-	command := NewFizzBuzzValueCommand(typeImpl)
+	base := fizzbuzztype.FizzBuzzTypeBase{}
+	typeImpl := base.Create(fizzbuzztype.TYPE_01)
+	command := application.NewFizzBuzzValueCommand(typeImpl)
 	command.Execute(-1)
 }
 
@@ -211,7 +216,7 @@ func Test値は正の値のみ許可する_FizzBuzzValueCommand(t *testing.T) {
 func Test100より多い数を許可しない_FizzBuzzListCommand(t *testing.T) {
 	defer func() {
 		if r := recover(); r != nil {
-			if err, ok := r.(InvalidValueError); ok {
+			if err, ok := r.(model.InvalidValueError); ok {
 				expected := "100 より多い数を許可しない"
 				if err.Error() != expected {
 					t.Errorf("Expected panic message %v, got %v", expected, err.Error())
@@ -224,9 +229,9 @@ func Test100より多い数を許可しない_FizzBuzzListCommand(t *testing.T)
 		}
 	}()
 	
-	base := FizzBuzzTypeBase{}
-	typeImpl := base.Create(TYPE_01)
-	command := NewFizzBuzzListCommand(typeImpl)
+	base := fizzbuzztype.FizzBuzzTypeBase{}
+	typeImpl := base.Create(fizzbuzztype.TYPE_01)
+	command := application.NewFizzBuzzListCommand(typeImpl)
 	command.Execute(101)
 }
 

```

## コミット: daeb7c1

### メッセージ

```
test: それ以外のタイプの場合（基本的な例外処理）
```

### 変更されたファイル

- M	app/fizzbuzz.go
- A	app/fizzbuzz_test
- M	app/main_test.go

### 変更内容

```diff
commit daeb7c141250c892c05741adf055a5619b91f1ef
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 08:14:18 2025 +0000

    test: それ以外のタイプの場合（基本的な例外処理）

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 520d26a..e32c1bf 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -314,4 +314,33 @@ func (c *FizzBuzzListCommand) Execute(number int) interface{} {
 	return NewFizzBuzzList(values).Value()
 }
 
+// BasicFizzBuzzGenerate 基本的なFizzBuzz生成関数（ドキュメントの初期バージョン）
+func BasicFizzBuzzGenerate(number, fizzBuzzType int) string {
+	isFizz := number%3 == 0
+	isBuzz := number%5 == 0
+
+	switch fizzBuzzType {
+	case 1:
+		if isFizz && isBuzz {
+			return "FizzBuzz"
+		}
+		if isFizz {
+			return "Fizz"
+		}
+		if isBuzz {
+			return "Buzz"
+		}
+		return strconv.Itoa(number)
+	case 2:
+		return strconv.Itoa(number)
+	case 3:
+		if isFizz && isBuzz {
+			return "FizzBuzz"
+		}
+		return strconv.Itoa(number)
+	default:
+		panic("該当するタイプは存在しません")
+	}
+}
+
 
diff --git a/app/fizzbuzz_test b/app/fizzbuzz_test
new file mode 100644
index 0000000..2c8b980
Binary files /dev/null and b/app/fizzbuzz_test differ
diff --git a/app/main_test.go b/app/main_test.go
index 3882aac..50dec2b 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -229,3 +229,20 @@ func Test100より多い数を許可しない_FizzBuzzListCommand(t *testing.T)
 	command := NewFizzBuzzListCommand(typeImpl)
 	command.Execute(101)
 }
+
+// 基本的な例外処理テスト - ドキュメントの手順に従った実装
+func Test基本例外処理_それ以外のタイプの場合(t *testing.T) {
+	defer func() {
+		if r := recover(); r != nil {
+			expected := "該当するタイプは存在しません"
+			if r != expected {
+				t.Errorf("Expected panic message %v, got %v", expected, r)
+			}
+		} else {
+			t.Error("Expected panic but no panic occurred")
+		}
+	}()
+	
+	// 存在しないタイプ4を指定
+	BasicFizzBuzzGenerate(1, 4)
+}

```

## コミット: bf26a7d

### メッセージ

```
refactor: 特殊ケースの導入（Null Objectパターン）
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/main_test.go

### 変更内容

```diff
commit bf26a7d69fd07c2ef51a3ea12c9e9086ce308ab4
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 08:05:57 2025 +0000

    refactor: 特殊ケースの導入（Null Objectパターン）

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index c50dfa6..520d26a 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -81,7 +81,7 @@ func (f FizzBuzzTypeBase) Create(fizzBuzzType int) FizzBuzzType {
 	case TYPE_03:
 		return FizzBuzzType03{}
 	default:
-		panic("該当するタイプは存在しません")
+		return FizzBuzzTypeNotDefined{}
 	}
 }
 
@@ -138,6 +138,19 @@ func (f FizzBuzzType03) Generate(number int) FizzBuzzValue {
 	return NewFizzBuzzValue(number, strconv.Itoa(number))
 }
 
+// FizzBuzzTypeNotDefined 未定義タイプの実装（Null Objectパターン）
+type FizzBuzzTypeNotDefined struct {
+	FizzBuzzTypeBase
+}
+
+func (f FizzBuzzTypeNotDefined) Generate(number int) FizzBuzzValue {
+	return NewFizzBuzzValue(number, "")
+}
+
+func (f FizzBuzzTypeNotDefined) String() string {
+	return "未定義"
+}
+
 // FizzBuzz構造体
 type FizzBuzz struct {
 	list         *FizzBuzzList // FizzBuzzListを保持するフィールド
@@ -150,9 +163,15 @@ func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
 	base := FizzBuzzTypeBase{}
 	typeImpl := base.Create(fizzBuzzType)
 
+	// 未定義タイプの場合は-1を設定
+	actualType := fizzBuzzType
+	if _, ok := typeImpl.(FizzBuzzTypeNotDefined); ok {
+		actualType = -1
+	}
+
 	return &FizzBuzz{
 		list:         NewFizzBuzzList([]FizzBuzzValue{}),
-		fizzBuzzType: fizzBuzzType,
+		fizzBuzzType: actualType,
 		typeImpl:     typeImpl,
 	}
 }
@@ -168,8 +187,10 @@ func NewFizzBuzzWithType(typeImpl FizzBuzzType) *FizzBuzz {
 		fizzBuzzType = TYPE_02
 	case FizzBuzzType03:
 		fizzBuzzType = TYPE_03
+	case FizzBuzzTypeNotDefined:
+		fizzBuzzType = -1 // 未定義タイプを示すデフォルト値
 	default:
-		panic("該当するタイプは存在しません")
+		fizzBuzzType = -1 // 未定義タイプを示すデフォルト値
 	}
 
 	return &FizzBuzz{
diff --git a/app/main_test.go b/app/main_test.go
index 735ba0b..3882aac 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -107,31 +107,34 @@ func Test15を渡したら文字列FizzBuzzを返す_タイプ3(t *testing.T) {
 }
 
 // それ以外のタイプの場合
-func TestGenerateByType_それ以外のタイプで例外が発生する(t *testing.T) {
-	defer func() {
-		if r := recover(); r == nil {
-			t.Errorf("FizzBuzzTypeBase.Create(4) should panic")
-		}
-	}()
+func TestGenerateByType_それ以外のタイプで未定義のタイプを返す(t *testing.T) {
 	base := FizzBuzzTypeBase{}
-	base.Create(4)
+	typeImpl := base.Create(4)
+	
+	// String()メソッドをテスト
+	if notDefined, ok := typeImpl.(FizzBuzzTypeNotDefined); ok {
+		got := notDefined.String()
+		expected := "未定義"
+		if got != expected {
+			t.Errorf("FizzBuzzTypeNotDefined.String() = %v, want %v", got, expected)
+		}
+	} else {
+		t.Error("Expected FizzBuzzTypeNotDefined type")
+	}
 }
 
 // それ以外のタイプの場合
-func Test該当しないタイプを指定した場合例外を返す(t *testing.T) {
-	defer func() {
-		if r := recover(); r != nil {
-			expected := "該当するタイプは存在しません"
-			if r != expected {
-				t.Errorf("Expected panic message %v, got %v", expected, r)
-			}
-		} else {
-			t.Error("Expected panic but no panic occurred")
-		}
-	}()
+func Test該当しないタイプを指定した場合未定義のタイプを返す(t *testing.T) {
+	fizzbuzz := NewFizzBuzz(4)
+	if fizzbuzz.Type() != -1 {
+		t.Errorf("Expected type -1 for undefined type, got %v", fizzbuzz.Type())
+	}
 	
-	// タイプ4（存在しないタイプ）を指定
-	NewFizzBuzz(4)
+	// Generate メソッドで空文字列が返されることを確認
+	result := fizzbuzz.Generate(1)
+	if result.Value() != "" {
+		t.Errorf("Expected empty string for undefined type, got %v", result.Value())
+	}
 }
 
 // 値オブジェクトのテスト

```

## コミット: 014a783

### メッセージ

```
refactor: マジックナンバーの置き換え
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/main_test.go

### 変更内容

```diff
commit 014a7833b91f2a66153af1affcbaa2ce18c7e800
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 08:03:09 2025 +0000

    refactor: マジックナンバーの置き換え

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index a55b4c0..c50dfa6 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -282,8 +282,9 @@ func NewFizzBuzzListCommand(typeImpl FizzBuzzType) *FizzBuzzListCommand {
 
 // Execute 指定した数までのFizzBuzzリストを生成して返す
 func (c *FizzBuzzListCommand) Execute(number int) interface{} {
-	if number > 100 {
-		panic(NewInvalidValueError("100より多い数を許可しない"))
+	const MaxCount = 100
+	if number > MaxCount {
+		panic(NewInvalidValueError(fmt.Sprintf("%d より多い数を許可しない", MaxCount)))
 	}
 	values := make([]FizzBuzzValue, 0, number)
 	for i := 1; i <= number; i++ {
diff --git a/app/main_test.go b/app/main_test.go
index 907800a..735ba0b 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -209,7 +209,7 @@ func Test100より多い数を許可しない_FizzBuzzListCommand(t *testing.T)
 	defer func() {
 		if r := recover(); r != nil {
 			if err, ok := r.(InvalidValueError); ok {
-				expected := "100より多い数を許可しない"
+				expected := "100 より多い数を許可しない"
 				if err.Error() != expected {
 					t.Errorf("Expected panic message %v, got %v", expected, err.Error())
 				}

```

## コミット: 1a0f6d0

### メッセージ

```
refactor: 例外によるエラーコードの置き換え
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/main_test.go

### 変更内容

```diff
commit 1a0f6d0da5da975379108e98fd957d6b34d83ad5
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 07:58:26 2025 +0000

    refactor: 例外によるエラーコードの置き換え

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index ec47124..a55b4c0 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -5,25 +5,18 @@ import (
 	"strconv"
 )
 
-// AssertionFailedError カスタムエラー型
-type AssertionFailedError struct {
+// InvalidValueError 不正な値エラー
+type InvalidValueError struct {
 	message string
 }
 
-func (e AssertionFailedError) Error() string {
+func (e InvalidValueError) Error() string {
 	return e.message
 }
 
-// NewAssertionFailedError コンストラクタ
-func NewAssertionFailedError(message string) AssertionFailedError {
-	return AssertionFailedError{message: message}
-}
-
-// Assert アサーション関数
-func Assert(condition bool, message string) {
-	if !condition {
-		panic(NewAssertionFailedError(message))
-	}
+// NewInvalidValueError コンストラクタ
+func NewInvalidValueError(message string) InvalidValueError {
+	return InvalidValueError{message: message}
 }
 
 // FizzBuzzValue 値オブジェクト
@@ -34,7 +27,9 @@ type FizzBuzzValue struct {
 
 // NewFizzBuzzValue コンストラクタ
 func NewFizzBuzzValue(number int, value string) FizzBuzzValue {
-	Assert(number >= 0, "値は正の値のみ許可")
+	if number < 0 {
+		panic(NewInvalidValueError("値は正の値のみ許可"))
+	}
 	return FizzBuzzValue{
 		number: number,
 		value:  value,
@@ -287,7 +282,9 @@ func NewFizzBuzzListCommand(typeImpl FizzBuzzType) *FizzBuzzListCommand {
 
 // Execute 指定した数までのFizzBuzzリストを生成して返す
 func (c *FizzBuzzListCommand) Execute(number int) interface{} {
-	Assert(number <= 100, "100より多い数を許可しない")
+	if number > 100 {
+		panic(NewInvalidValueError("100より多い数を許可しない"))
+	}
 	values := make([]FizzBuzzValue, 0, number)
 	for i := 1; i <= number; i++ {
 		values = append(values, c.typeImpl.Generate(i))
diff --git a/app/main_test.go b/app/main_test.go
index df8cccd..907800a 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -185,13 +185,13 @@ func TestFizzBuzzList新しいインスタンスが作られる(t *testing.T) {
 func Test値は正の値のみ許可する_FizzBuzzValueCommand(t *testing.T) {
 	defer func() {
 		if r := recover(); r != nil {
-			if err, ok := r.(AssertionFailedError); ok {
+			if err, ok := r.(InvalidValueError); ok {
 				expected := "値は正の値のみ許可"
 				if err.Error() != expected {
 					t.Errorf("Expected panic message %v, got %v", expected, err.Error())
 				}
 			} else {
-				t.Errorf("Expected AssertionFailedError but got %v", r)
+				t.Errorf("Expected InvalidValueError but got %v", r)
 			}
 		} else {
 			t.Error("Expected panic but no panic occurred")
@@ -208,13 +208,13 @@ func Test値は正の値のみ許可する_FizzBuzzValueCommand(t *testing.T) {
 func Test100より多い数を許可しない_FizzBuzzListCommand(t *testing.T) {
 	defer func() {
 		if r := recover(); r != nil {
-			if err, ok := r.(AssertionFailedError); ok {
+			if err, ok := r.(InvalidValueError); ok {
 				expected := "100より多い数を許可しない"
 				if err.Error() != expected {
 					t.Errorf("Expected panic message %v, got %v", expected, err.Error())
 				}
 			} else {
-				t.Errorf("Expected AssertionFailedError but got %v", r)
+				t.Errorf("Expected InvalidValueError but got %v", r)
 			}
 		} else {
 			t.Error("Expected panic but no panic occurred")

```

## コミット: 2713a7b

### メッセージ

```
refactor: アサーションの導入
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/main_test.go

### 変更内容

```diff
commit 2713a7b26b8afc8bb2a907ccbaa799fef9d345d1
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 07:56:58 2025 +0000

    refactor: アサーションの導入

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index dc040b7..ec47124 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -5,6 +5,27 @@ import (
 	"strconv"
 )
 
+// AssertionFailedError カスタムエラー型
+type AssertionFailedError struct {
+	message string
+}
+
+func (e AssertionFailedError) Error() string {
+	return e.message
+}
+
+// NewAssertionFailedError コンストラクタ
+func NewAssertionFailedError(message string) AssertionFailedError {
+	return AssertionFailedError{message: message}
+}
+
+// Assert アサーション関数
+func Assert(condition bool, message string) {
+	if !condition {
+		panic(NewAssertionFailedError(message))
+	}
+}
+
 // FizzBuzzValue 値オブジェクト
 type FizzBuzzValue struct {
 	number int
@@ -13,6 +34,7 @@ type FizzBuzzValue struct {
 
 // NewFizzBuzzValue コンストラクタ
 func NewFizzBuzzValue(number int, value string) FizzBuzzValue {
+	Assert(number >= 0, "値は正の値のみ許可")
 	return FizzBuzzValue{
 		number: number,
 		value:  value,
@@ -265,6 +287,7 @@ func NewFizzBuzzListCommand(typeImpl FizzBuzzType) *FizzBuzzListCommand {
 
 // Execute 指定した数までのFizzBuzzリストを生成して返す
 func (c *FizzBuzzListCommand) Execute(number int) interface{} {
+	Assert(number <= 100, "100より多い数を許可しない")
 	values := make([]FizzBuzzValue, 0, number)
 	for i := 1; i <= number; i++ {
 		values = append(values, c.typeImpl.Generate(i))
diff --git a/app/main_test.go b/app/main_test.go
index 2f0f755..df8cccd 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -166,15 +166,63 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ3(t *testing.T) {
 func TestFizzBuzzList新しいインスタンスが作られる(t *testing.T) {
 	typeInstance := FizzBuzzType01{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
-	fizzbuzz.GenerateList(1, 100)
+	fizzbuzz.GenerateList(1, 50)
 	
 	list1 := fizzbuzz.list
 	list2 := list1.Add(list1.Value())
 	
-	if list1.Count() != 100 {
-		t.Errorf("list1.Count() = %d, want 100", list1.Count())
+	if list1.Count() != 50 {
+		t.Errorf("list1.Count() = %d, want 50", list1.Count())
 	}
-	if list2.Count() != 200 {
-		t.Errorf("list2.Count() = %d, want 200", list2.Count())
+	if list2.Count() != 100 {
+		t.Errorf("list2.Count() = %d, want 100", list2.Count())
 	}
 }
+
+// 例外ケース
+
+// 値は正の値のみ許可する
+func Test値は正の値のみ許可する_FizzBuzzValueCommand(t *testing.T) {
+	defer func() {
+		if r := recover(); r != nil {
+			if err, ok := r.(AssertionFailedError); ok {
+				expected := "値は正の値のみ許可"
+				if err.Error() != expected {
+					t.Errorf("Expected panic message %v, got %v", expected, err.Error())
+				}
+			} else {
+				t.Errorf("Expected AssertionFailedError but got %v", r)
+			}
+		} else {
+			t.Error("Expected panic but no panic occurred")
+		}
+	}()
+	
+	base := FizzBuzzTypeBase{}
+	typeImpl := base.Create(TYPE_01)
+	command := NewFizzBuzzValueCommand(typeImpl)
+	command.Execute(-1)
+}
+
+// 100より多い数を許可しない
+func Test100より多い数を許可しない_FizzBuzzListCommand(t *testing.T) {
+	defer func() {
+		if r := recover(); r != nil {
+			if err, ok := r.(AssertionFailedError); ok {
+				expected := "100より多い数を許可しない"
+				if err.Error() != expected {
+					t.Errorf("Expected panic message %v, got %v", expected, err.Error())
+				}
+			} else {
+				t.Errorf("Expected AssertionFailedError but got %v", r)
+			}
+		} else {
+			t.Error("Expected panic but no panic occurred")
+		}
+	}()
+	
+	base := FizzBuzzTypeBase{}
+	typeImpl := base.Create(TYPE_01)
+	command := NewFizzBuzzListCommand(typeImpl)
+	command.Execute(101)
+}

```

## コミット: 0b7f373

### メッセージ

```
refactor: メソッドオブジェクトによるメソッドの置き換え（Commandパターン実装済み）
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/main_test.go

### 変更内容

```diff
commit 0b7f37316dd749061b6509ba7802aefd41921d95
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 07:53:59 2025 +0000

    refactor: メソッドオブジェクトによるメソッドの置き換え（Commandパターン実装済み）

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 570578b..dc040b7 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -229,4 +229,47 @@ func (f *FizzBuzzList) Count() int {
 	return len(f.value)
 }
 
+// FizzBuzzCommand Commandパターンのインターフェース
+type FizzBuzzCommand interface {
+	Execute(number int) interface{}
+}
+
+// FizzBuzzValueCommand 値オブジェクトを返すコマンド
+type FizzBuzzValueCommand struct {
+	typeImpl FizzBuzzType
+}
+
+// NewFizzBuzzValueCommand コンストラクタ
+func NewFizzBuzzValueCommand(typeImpl FizzBuzzType) *FizzBuzzValueCommand {
+	return &FizzBuzzValueCommand{
+		typeImpl: typeImpl,
+	}
+}
+
+// Execute 値オブジェクトの値部分を返す
+func (c *FizzBuzzValueCommand) Execute(number int) interface{} {
+	return c.typeImpl.Generate(number).Value()
+}
+
+// FizzBuzzListCommand ファーストクラスコレクションを返すコマンド
+type FizzBuzzListCommand struct {
+	typeImpl FizzBuzzType
+}
+
+// NewFizzBuzzListCommand コンストラクタ
+func NewFizzBuzzListCommand(typeImpl FizzBuzzType) *FizzBuzzListCommand {
+	return &FizzBuzzListCommand{
+		typeImpl: typeImpl,
+	}
+}
+
+// Execute 指定した数までのFizzBuzzリストを生成して返す
+func (c *FizzBuzzListCommand) Execute(number int) interface{} {
+	values := make([]FizzBuzzValue, 0, number)
+	for i := 1; i <= number; i++ {
+		values = append(values, c.typeImpl.Generate(i))
+	}
+	return NewFizzBuzzList(values).Value()
+}
+
 
diff --git a/app/main_test.go b/app/main_test.go
index 89e19ff..2f0f755 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -6,10 +6,10 @@ func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected strin
 	t.Helper()
 	base := FizzBuzzTypeBase{}
 	typeImpl := base.Create(fizzBuzzType)
-	fizzbuzz := NewFizzBuzzWithType(typeImpl)
-	got := fizzbuzz.Generate(number)
-	if got.Value() != expected {
-		t.Errorf("FizzBuzz.Generate(%d) with type %d = %v, want %v", number, fizzBuzzType, got.Value(), expected)
+	command := NewFizzBuzzValueCommand(typeImpl)
+	got := command.Execute(number).(string)
+	if got != expected {
+		t.Errorf("FizzBuzzValueCommand.Execute(%d) with type %d = %v, want %v", number, fizzBuzzType, got, expected)
 	}
 }
 
@@ -39,9 +39,8 @@ func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
 // 1から100までのFizzBuzzの配列を返す
 func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
 	typeImpl := FizzBuzzType01{}
-	fizzbuzz := NewFizzBuzzWithType(typeImpl)
-	fizzbuzz.GenerateList(1, 100)
-	results := fizzbuzz.List()
+	command := NewFizzBuzzListCommand(typeImpl)
+	results := command.Execute(100).([]FizzBuzzValue)
 
 	// いくつかの値を確認
 	if len(results) != 100 {

```

## コミット: 8c837fc

### メッセージ

```
refactor: 学習用テスト
- FizzBuzzListファーストクラスコレクションの動作を確認する学習用テストを追加
- Add操作で新しいインスタンスが作られることを確認
- イミュータブルな設計によりlist1とlist2が独立していることを検証
- Count()メソッドで要素数を正しく取得できることを確認
- 全テスト通過を確認
```

### 変更されたファイル

- M	app/main_test.go

### 変更内容

```diff
commit 8c837fc4b2c7f48ce733c9b87c0a7b5626ab3259
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 07:08:30 2025 +0000

    refactor: 学習用テスト
    
    - FizzBuzzListファーストクラスコレクションの動作を確認する学習用テストを追加
    - Add操作で新しいインスタンスが作られることを確認
    - イミュータブルな設計によりlist1とlist2が独立していることを検証
    - Count()メソッドで要素数を正しく取得できることを確認
    - 全テスト通過を確認

diff --git a/app/main_test.go b/app/main_test.go
index 1e9ad9d..89e19ff 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -162,3 +162,20 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ3(t *testing.T) {
 		t.Errorf("FizzBuzz with value object type = %v, want FizzBuzz", got.Value())
 	}
 }
+
+// FizzBuzzListの学習用テスト
+func TestFizzBuzzList新しいインスタンスが作られる(t *testing.T) {
+	typeInstance := FizzBuzzType01{}
+	fizzbuzz := NewFizzBuzzWithType(typeInstance)
+	fizzbuzz.GenerateList(1, 100)
+	
+	list1 := fizzbuzz.list
+	list2 := list1.Add(list1.Value())
+	
+	if list1.Count() != 100 {
+		t.Errorf("list1.Count() = %d, want 100", list1.Count())
+	}
+	if list2.Count() != 200 {
+		t.Errorf("list2.Count() = %d, want 200", list2.Count())
+	}
+}

```

## コミット: ffb053e

### メッセージ

```
refactor: コレクションのカプセル化
- FizzBuzzListファーストクラスコレクションを追加
- []FizzBuzzValueを直接操作する代わりにFizzBuzzListを使用
- イミュータブルな設計でAdd操作は新しいインスタンスを返す
- 外部からの直接アクセスを防ぐためValue()メソッドでコピーを返す
- FizzBuzz構造体のlistフィールドをFizzBuzzListに変更
- GenerateListメソッドでAdd操作を使用してイミュータブルに更新
- 全テスト通過とメインプログラム正常動作を確認
```

### 変更されたファイル

- M	app/fizzbuzz.go

### 変更内容

```diff
commit ffb053ee28e523bddca8fd2a0cc8f605d4c9b61c
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 07:05:31 2025 +0000

    refactor: コレクションのカプセル化
    
    - FizzBuzzListファーストクラスコレクションを追加
    - []FizzBuzzValueを直接操作する代わりにFizzBuzzListを使用
    - イミュータブルな設計でAdd操作は新しいインスタンスを返す
    - 外部からの直接アクセスを防ぐためValue()メソッドでコピーを返す
    - FizzBuzz構造体のlistフィールドをFizzBuzzListに変更
    - GenerateListメソッドでAdd操作を使用してイミュータブルに更新
    - 全テスト通過とメインプログラム正常動作を確認

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 2e17db9..570578b 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -123,9 +123,9 @@ func (f FizzBuzzType03) Generate(number int) FizzBuzzValue {
 
 // FizzBuzz構造体
 type FizzBuzz struct {
-	list         []FizzBuzzValue // FizzBuzz配列を保持するフィールド
-	fizzBuzzType int             // FizzBuzzのタイプを保持するフィールド
-	typeImpl     FizzBuzzType    // タイプ実装
+	list         *FizzBuzzList // FizzBuzzListを保持するフィールド
+	fizzBuzzType int           // FizzBuzzのタイプを保持するフィールド
+	typeImpl     FizzBuzzType  // タイプ実装
 }
 
 // NewFizzBuzz コンストラクタ（プリミティブ型を受け取る）
@@ -134,7 +134,7 @@ func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
 	typeImpl := base.Create(fizzBuzzType)
 
 	return &FizzBuzz{
-		list:         make([]FizzBuzzValue, 0),
+		list:         NewFizzBuzzList([]FizzBuzzValue{}),
 		fizzBuzzType: fizzBuzzType,
 		typeImpl:     typeImpl,
 	}
@@ -156,7 +156,7 @@ func NewFizzBuzzWithType(typeImpl FizzBuzzType) *FizzBuzz {
 	}
 
 	return &FizzBuzz{
-		list:         make([]FizzBuzzValue, 0),
+		list:         NewFizzBuzzList([]FizzBuzzValue{}),
 		fizzBuzzType: fizzBuzzType,
 		typeImpl:     typeImpl,
 	}
@@ -174,15 +174,59 @@ func (f *FizzBuzz) Type() int {
 
 // List FizzBuzz配列を取得
 func (f *FizzBuzz) List() []FizzBuzzValue {
-	return f.list
+	return f.list.Value()
 }
 
 // GenerateList 範囲指定してFizzBuzzのリストを作成し、インスタンス変数に保存
 func (f *FizzBuzz) GenerateList(start, end int) {
-	f.list = make([]FizzBuzzValue, 0, end-start+1)
+	values := make([]FizzBuzzValue, 0, end-start+1)
 	for i := start; i <= end; i++ {
-		f.list = append(f.list, f.Generate(i))
+		values = append(values, f.Generate(i))
 	}
+	f.list = f.list.Add(values)
+}
+
+// FizzBuzzList ファーストクラスコレクション
+type FizzBuzzList struct {
+	value []FizzBuzzValue
+}
+
+// NewFizzBuzzList コンストラクタ
+func NewFizzBuzzList(values []FizzBuzzValue) *FizzBuzzList {
+	// イミュータブルにするため新しいスライスを作成
+	newValues := make([]FizzBuzzValue, len(values))
+	copy(newValues, values)
+	return &FizzBuzzList{
+		value: newValues,
+	}
+}
+
+// Value 値を取得（読み取り専用）
+func (f *FizzBuzzList) Value() []FizzBuzzValue {
+	// 外部からの変更を防ぐためコピーを返す
+	result := make([]FizzBuzzValue, len(f.value))
+	copy(result, f.value)
+	return result
+}
+
+// String 文字列表現
+func (f *FizzBuzzList) String() string {
+	return fmt.Sprintf("%v", f.value)
+}
+
+// Add 新しい要素を追加した新しいFizzBuzzListを返す（イミュータブル）
+func (f *FizzBuzzList) Add(values []FizzBuzzValue) *FizzBuzzList {
+	newValues := make([]FizzBuzzValue, len(f.value)+len(values))
+	copy(newValues, f.value)
+	copy(newValues[len(f.value):], values)
+	return &FizzBuzzList{
+		value: newValues,
+	}
+}
+
+// Count 要素数を取得
+func (f *FizzBuzzList) Count() int {
+	return len(f.value)
 }
 
 

```

## コミット: fb08022

### メッセージ

```
refactor: フィールドのカプセル化
- 後方互換性のためのラッパー関数（Generate, GenerateByType, GenerateList）を削除
- 完全にインスタンスベースのAPIに統一
- すべてのアクセスはFizzBuzz構造体のインスタンスメソッド経由に一本化
- フィールドは非公開でアクセッサメソッド経由でのみアクセス可能
- テスト全通過とメインプログラム正常動作を確認
```

### 変更されたファイル

- M	app/fizzbuzz.go

### 変更内容

```diff
commit fb08022413ee885a0d9f6d1510a591efc713b964
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:49:25 2025 +0000

    refactor: フィールドのカプセル化
    
    - 後方互換性のためのラッパー関数（Generate, GenerateByType, GenerateList）を削除
    - 完全にインスタンスベースのAPIに統一
    - すべてのアクセスはFizzBuzz構造体のインスタンスメソッド経由に一本化
    - フィールドは非公開でアクセッサメソッド経由でのみアクセス可能
    - テスト全通過とメインプログラム正常動作を確認

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index ff7104b..2e17db9 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -185,24 +185,4 @@ func (f *FizzBuzz) GenerateList(start, end int) {
 	}
 }
 
-// 後方互換性のためのラッパー関数
-func Generate(number int) string {
-	fizzbuzz := NewFizzBuzz(1)
-	return fizzbuzz.Generate(number).Value()
-}
-
-func GenerateByType(number, fizzBuzzType int) string {
-	fizzbuzz := NewFizzBuzz(fizzBuzzType)
-	return fizzbuzz.Generate(number).Value()
-}
 
-func GenerateList(start, end int) []string {
-	fizzbuzz := NewFizzBuzz(1)
-	fizzbuzz.GenerateList(start, end)
-	results := fizzbuzz.List()
-	strResults := make([]string, len(results))
-	for i, result := range results {
-		strResults[i] = result.Value()
-	}
-	return strResults
-}

```

## コミット: 8dcb359

### メッセージ

```
test: それ以外のタイプの場合
- 存在しないタイプを指定した場合のpanicテストを追加
- defer recover()でパニックメッセージを検証
- 既存のファクトリメソッドで適切に例外処理されることを確認
```

### 変更されたファイル

- M	app/main_test.go

### 変更内容

```diff
commit 8dcb3596a2a545a83f5b5e371544bde9ba4a0725
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:37:56 2025 +0000

    test: それ以外のタイプの場合
    
    - 存在しないタイプを指定した場合のpanicテストを追加
    - defer recover()でパニックメッセージを検証
    - 既存のファクトリメソッドで適切に例外処理されることを確認

diff --git a/app/main_test.go b/app/main_test.go
index dfb2898..1e9ad9d 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -118,6 +118,23 @@ func TestGenerateByType_それ以外のタイプで例外が発生する(t *test
 	base.Create(4)
 }
 
+// それ以外のタイプの場合
+func Test該当しないタイプを指定した場合例外を返す(t *testing.T) {
+	defer func() {
+		if r := recover(); r != nil {
+			expected := "該当するタイプは存在しません"
+			if r != expected {
+				t.Errorf("Expected panic message %v, got %v", expected, r)
+			}
+		} else {
+			t.Error("Expected panic but no panic occurred")
+		}
+	}()
+	
+	// タイプ4（存在しないタイプ）を指定
+	NewFizzBuzz(4)
+}
+
 // 値オブジェクトのテスト
 func Test値オブジェクトを使用したFizzBuzz_タイプ1(t *testing.T) {
 	typeInstance := FizzBuzzType01{}

```

## コミット: 97b464e

### メッセージ

```
値オブジェクトFizzBuzzValueの使用を完了し、全ての関数・メソッドを更新
- FizzBuzz構造体のlistフィールドを[]FizzBuzzValueに変更
- Generate、List、GenerateListメソッドをFizzBuzzValue対応に更新
- 後方互換性のためのラッパー関数でValue()メソッドを使用
- main関数をmain.goに移動してFizzBuzzValueに対応
- テスト関数をFizzBuzzValue.Value()を使用するよう更新
- 全テスト通過とメインプログラム正常動作を確認
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/go-tdd-tutorial
- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 97b464e330a7160411b29c04b59f34a9d823a974
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:33:51 2025 +0000

    値オブジェクトFizzBuzzValueの使用を完了し、全ての関数・メソッドを更新
    
    - FizzBuzz構造体のlistフィールドを[]FizzBuzzValueに変更
    - Generate、List、GenerateListメソッドをFizzBuzzValue対応に更新
    - 後方互換性のためのラッパー関数でValue()メソッドを使用
    - main関数をmain.goに移動してFizzBuzzValueに対応
    - テスト関数をFizzBuzzValue.Value()を使用するよう更新
    - 全テスト通過とメインプログラム正常動作を確認

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 705d12f..ff7104b 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -5,9 +5,43 @@ import (
 	"strconv"
 )
 
+// FizzBuzzValue 値オブジェクト
+type FizzBuzzValue struct {
+	number int
+	value  string
+}
+
+// NewFizzBuzzValue コンストラクタ
+func NewFizzBuzzValue(number int, value string) FizzBuzzValue {
+	return FizzBuzzValue{
+		number: number,
+		value:  value,
+	}
+}
+
+// Number 数値を取得
+func (f FizzBuzzValue) Number() int {
+	return f.number
+}
+
+// Value 値を取得
+func (f FizzBuzzValue) Value() string {
+	return f.value
+}
+
+// String 文字列表現
+func (f FizzBuzzValue) String() string {
+	return fmt.Sprintf("%d:%s", f.number, f.value)
+}
+
+// Equal 等価性チェック
+func (f FizzBuzzValue) Equal(other FizzBuzzValue) bool {
+	return f.number == other.number && f.value == other.value
+}
+
 // FizzBuzzType インターフェース
 type FizzBuzzType interface {
-	Generate(number int) string
+	Generate(number int) FizzBuzzValue
 }
 
 // FizzBuzzTypeBase 共通処理を提供する基底構造体
@@ -47,20 +81,20 @@ type FizzBuzzType01 struct {
 	FizzBuzzTypeBase
 }
 
-func (f FizzBuzzType01) Generate(number int) string {
+func (f FizzBuzzType01) Generate(number int) FizzBuzzValue {
 	isFizz := f.Fizz(number)
 	isBuzz := f.Buzz(number)
 
 	if isFizz && isBuzz {
-		return "FizzBuzz"
+		return NewFizzBuzzValue(number, "FizzBuzz")
 	}
 	if isFizz {
-		return "Fizz"
+		return NewFizzBuzzValue(number, "Fizz")
 	}
 	if isBuzz {
-		return "Buzz"
+		return NewFizzBuzzValue(number, "Buzz")
 	}
-	return strconv.Itoa(number)
+	return NewFizzBuzzValue(number, strconv.Itoa(number))
 }
 
 // FizzBuzzType02 タイプ2の実装
@@ -68,8 +102,8 @@ type FizzBuzzType02 struct {
 	FizzBuzzTypeBase
 }
 
-func (f FizzBuzzType02) Generate(number int) string {
-	return strconv.Itoa(number)
+func (f FizzBuzzType02) Generate(number int) FizzBuzzValue {
+	return NewFizzBuzzValue(number, strconv.Itoa(number))
 }
 
 // FizzBuzzType03 タイプ3の実装
@@ -77,32 +111,32 @@ type FizzBuzzType03 struct {
 	FizzBuzzTypeBase
 }
 
-func (f FizzBuzzType03) Generate(number int) string {
+func (f FizzBuzzType03) Generate(number int) FizzBuzzValue {
 	isFizz := f.Fizz(number)
 	isBuzz := f.Buzz(number)
 
 	if isFizz && isBuzz {
-		return "FizzBuzz"
+		return NewFizzBuzzValue(number, "FizzBuzz")
 	}
-	return strconv.Itoa(number)
+	return NewFizzBuzzValue(number, strconv.Itoa(number))
 }
 
 // FizzBuzz構造体
 type FizzBuzz struct {
-	list        []string      // FizzBuzz配列を保持するフィールド
-	fizzBuzzType int          // FizzBuzzのタイプを保持するフィールド
-	typeImpl    FizzBuzzType  // タイプ実装
+	list         []FizzBuzzValue // FizzBuzz配列を保持するフィールド
+	fizzBuzzType int             // FizzBuzzのタイプを保持するフィールド
+	typeImpl     FizzBuzzType    // タイプ実装
 }
 
 // NewFizzBuzz コンストラクタ（プリミティブ型を受け取る）
 func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
 	base := FizzBuzzTypeBase{}
 	typeImpl := base.Create(fizzBuzzType)
-	
+
 	return &FizzBuzz{
-		list:        make([]string, 0),
+		list:         make([]FizzBuzzValue, 0),
 		fizzBuzzType: fizzBuzzType,
-		typeImpl:    typeImpl,
+		typeImpl:     typeImpl,
 	}
 }
 
@@ -120,16 +154,16 @@ func NewFizzBuzzWithType(typeImpl FizzBuzzType) *FizzBuzz {
 	default:
 		panic("該当するタイプは存在しません")
 	}
-	
+
 	return &FizzBuzz{
-		list:        make([]string, 0),
+		list:         make([]FizzBuzzValue, 0),
 		fizzBuzzType: fizzBuzzType,
-		typeImpl:    typeImpl,
+		typeImpl:     typeImpl,
 	}
 }
 
 // Generate 数を文字列に変換してFizzBuzzルールを適用
-func (f *FizzBuzz) Generate(number int) string {
+func (f *FizzBuzz) Generate(number int) FizzBuzzValue {
 	return f.typeImpl.Generate(number)
 }
 
@@ -139,13 +173,13 @@ func (f *FizzBuzz) Type() int {
 }
 
 // List FizzBuzz配列を取得
-func (f *FizzBuzz) List() []string {
+func (f *FizzBuzz) List() []FizzBuzzValue {
 	return f.list
 }
 
 // GenerateList 範囲指定してFizzBuzzのリストを作成し、インスタンス変数に保存
 func (f *FizzBuzz) GenerateList(start, end int) {
-	f.list = make([]string, 0, end-start+1)
+	f.list = make([]FizzBuzzValue, 0, end-start+1)
 	for i := start; i <= end; i++ {
 		f.list = append(f.list, f.Generate(i))
 	}
@@ -154,26 +188,21 @@ func (f *FizzBuzz) GenerateList(start, end int) {
 // 後方互換性のためのラッパー関数
 func Generate(number int) string {
 	fizzbuzz := NewFizzBuzz(1)
-	return fizzbuzz.Generate(number)
+	return fizzbuzz.Generate(number).Value()
 }
 
 func GenerateByType(number, fizzBuzzType int) string {
 	fizzbuzz := NewFizzBuzz(fizzBuzzType)
-	return fizzbuzz.Generate(number)
+	return fizzbuzz.Generate(number).Value()
 }
 
 func GenerateList(start, end int) []string {
 	fizzbuzz := NewFizzBuzz(1)
 	fizzbuzz.GenerateList(start, end)
-	return fizzbuzz.List()
-}
-
-func main() {
-	fmt.Println("FizzBuzz Game:")
-	fizzbuzz := NewFizzBuzz(1)
-	fizzbuzz.GenerateList(1, 100)
 	results := fizzbuzz.List()
-	for _, result := range results {
-		fmt.Println(result)
+	strResults := make([]string, len(results))
+	for i, result := range results {
+		strResults[i] = result.Value()
 	}
+	return strResults
 }
diff --git a/app/go-tdd-tutorial b/app/go-tdd-tutorial
index 7af0d0c..9865745 100644
Binary files a/app/go-tdd-tutorial and b/app/go-tdd-tutorial differ
diff --git a/app/main.go b/app/main.go
index 06ab7d0..aa9d680 100644
--- a/app/main.go
+++ b/app/main.go
@@ -1 +1,13 @@
 package main
+
+import "fmt"
+
+func main() {
+	fmt.Println("FizzBuzz Game:")
+	fizzbuzz := NewFizzBuzz(1)
+	fizzbuzz.GenerateList(1, 100)
+	results := fizzbuzz.List()
+	for _, result := range results {
+		fmt.Println(result.Value())
+	}
+}
diff --git a/app/main_test.go b/app/main_test.go
index 92737ae..dfb2898 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -8,8 +8,8 @@ func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected strin
 	typeImpl := base.Create(fizzBuzzType)
 	fizzbuzz := NewFizzBuzzWithType(typeImpl)
 	got := fizzbuzz.Generate(number)
-	if got != expected {
-		t.Errorf("FizzBuzz.Generate(%d) with type %d = %v, want %v", number, fizzBuzzType, got, expected)
+	if got.Value() != expected {
+		t.Errorf("FizzBuzz.Generate(%d) with type %d = %v, want %v", number, fizzBuzzType, got.Value(), expected)
 	}
 }
 
@@ -49,17 +49,17 @@ func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
 	}
 
 	// 具体的な値を確認
-	if results[0] != "1" {
-		t.Errorf("results[0] = %s, want 1", results[0])
+	if results[0].Value() != "1" {
+		t.Errorf("results[0] = %s, want 1", results[0].Value())
 	}
-	if results[2] != "Fizz" {
-		t.Errorf("results[2] = %s, want Fizz", results[2])
+	if results[2].Value() != "Fizz" {
+		t.Errorf("results[2] = %s, want Fizz", results[2].Value())
 	}
-	if results[4] != "Buzz" {
-		t.Errorf("results[4] = %s, want Buzz", results[4])
+	if results[4].Value() != "Buzz" {
+		t.Errorf("results[4] = %s, want Buzz", results[4].Value())
 	}
-	if results[14] != "FizzBuzz" {
-		t.Errorf("results[14] = %s, want FizzBuzz", results[14])
+	if results[14].Value() != "FizzBuzz" {
+		t.Errorf("results[14] = %s, want FizzBuzz", results[14].Value())
 	}
 }
 
@@ -123,8 +123,8 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ1(t *testing.T) {
 	typeInstance := FizzBuzzType01{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
 	got := fizzbuzz.Generate(3)
-	if got != "Fizz" {
-		t.Errorf("FizzBuzz with value object type = %v, want Fizz", got)
+	if got.Value() != "Fizz" {
+		t.Errorf("FizzBuzz with value object type = %v, want Fizz", got.Value())
 	}
 }
 
@@ -132,8 +132,8 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ2(t *testing.T) {
 	typeInstance := FizzBuzzType02{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
 	got := fizzbuzz.Generate(3)
-	if got != "3" {
-		t.Errorf("FizzBuzz with value object type = %v, want 3", got)
+	if got.Value() != "3" {
+		t.Errorf("FizzBuzz with value object type = %v, want 3", got.Value())
 	}
 }
 
@@ -141,7 +141,7 @@ func Test値オブジェクトを使用したFizzBuzz_タイプ3(t *testing.T) {
 	typeInstance := FizzBuzzType03{}
 	fizzbuzz := NewFizzBuzzWithType(typeInstance)
 	got := fizzbuzz.Generate(15)
-	if got != "FizzBuzz" {
-		t.Errorf("FizzBuzz with value object type = %v, want FizzBuzz", got)
+	if got.Value() != "FizzBuzz" {
+		t.Errorf("FizzBuzz with value object type = %v, want FizzBuzz", got.Value())
 	}
 }

```

## コミット: d489816

### メッセージ

```
refactor: マジックナンバーの置き換え
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/go-tdd-tutorial

### 変更内容

```diff
commit d489816304195371c228be9c6ddaa083925ce261
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:25:28 2025 +0000

    refactor: マジックナンバーの置き換え

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index c7c0e50..705d12f 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -13,14 +13,21 @@ type FizzBuzzType interface {
 // FizzBuzzTypeBase 共通処理を提供する基底構造体
 type FizzBuzzTypeBase struct{}
 
+// タイプ定数
+const (
+	TYPE_01 = 1
+	TYPE_02 = 2
+	TYPE_03 = 3
+)
+
 // Create ファクトリメソッド
 func (f FizzBuzzTypeBase) Create(fizzBuzzType int) FizzBuzzType {
 	switch fizzBuzzType {
-	case 1:
+	case TYPE_01:
 		return FizzBuzzType01{}
-	case 2:
+	case TYPE_02:
 		return FizzBuzzType02{}
-	case 3:
+	case TYPE_03:
 		return FizzBuzzType03{}
 	default:
 		panic("該当するタイプは存在しません")
@@ -105,11 +112,11 @@ func NewFizzBuzzWithType(typeImpl FizzBuzzType) *FizzBuzz {
 	var fizzBuzzType int
 	switch typeImpl.(type) {
 	case FizzBuzzType01:
-		fizzBuzzType = 1
+		fizzBuzzType = TYPE_01
 	case FizzBuzzType02:
-		fizzBuzzType = 2
+		fizzBuzzType = TYPE_02
 	case FizzBuzzType03:
-		fizzBuzzType = 3
+		fizzBuzzType = TYPE_03
 	default:
 		panic("該当するタイプは存在しません")
 	}
diff --git a/app/go-tdd-tutorial b/app/go-tdd-tutorial
index fa9b01a..7af0d0c 100644
Binary files a/app/go-tdd-tutorial and b/app/go-tdd-tutorial differ

```

## コミット: 62b104f

### メッセージ

```
refactor: テストを値オブジェクト使用に変更
```

### 変更されたファイル

- M	app/main_test.go

### 変更内容

```diff
commit 62b104f7518b7b46866cb73536bbb32159b806fa
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:22:37 2025 +0000

    refactor: テストを値オブジェクト使用に変更

diff --git a/app/main_test.go b/app/main_test.go
index fc60098..92737ae 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -4,7 +4,9 @@ import "testing"
 
 func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected string) {
 	t.Helper()
-	fizzbuzz := NewFizzBuzz(fizzBuzzType)
+	base := FizzBuzzTypeBase{}
+	typeImpl := base.Create(fizzBuzzType)
+	fizzbuzz := NewFizzBuzzWithType(typeImpl)
 	got := fizzbuzz.Generate(number)
 	if got != expected {
 		t.Errorf("FizzBuzz.Generate(%d) with type %d = %v, want %v", number, fizzBuzzType, got, expected)
@@ -36,7 +38,8 @@ func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
 
 // 1から100までのFizzBuzzの配列を返す
 func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
-	fizzbuzz := NewFizzBuzz(1)
+	typeImpl := FizzBuzzType01{}
+	fizzbuzz := NewFizzBuzzWithType(typeImpl)
 	fizzbuzz.GenerateList(1, 100)
 	results := fizzbuzz.List()
 
@@ -108,11 +111,11 @@ func Test15を渡したら文字列FizzBuzzを返す_タイプ3(t *testing.T) {
 func TestGenerateByType_それ以外のタイプで例外が発生する(t *testing.T) {
 	defer func() {
 		if r := recover(); r == nil {
-			t.Errorf("FizzBuzz.Generate(1) with type 4 should panic")
+			t.Errorf("FizzBuzzTypeBase.Create(4) should panic")
 		}
 	}()
-	fizzbuzz := NewFizzBuzz(4)
-	fizzbuzz.Generate(1)
+	base := FizzBuzzTypeBase{}
+	base.Create(4)
 }
 
 // 値オブジェクトのテスト

```

## コミット: d706272

### メッセージ

```
refactor: オブジェクトによるプリミティブの置き換え
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/go-tdd-tutorial
- M	app/main_test.go

### 変更内容

```diff
commit d70627222f35fc3ea6cb74a97eb54c725ffda7cf
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:20:01 2025 +0000

    refactor: オブジェクトによるプリミティブの置き換え

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 12964bc..c7c0e50 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -87,7 +87,7 @@ type FizzBuzz struct {
 	typeImpl    FizzBuzzType  // タイプ実装
 }
 
-// NewFizzBuzz コンストラクタ
+// NewFizzBuzz コンストラクタ（プリミティブ型を受け取る）
 func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
 	base := FizzBuzzTypeBase{}
 	typeImpl := base.Create(fizzBuzzType)
@@ -99,6 +99,28 @@ func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
 	}
 }
 
+// NewFizzBuzzWithType コンストラクタ（値オブジェクトを受け取る）
+func NewFizzBuzzWithType(typeImpl FizzBuzzType) *FizzBuzz {
+	// タイプを逆算（実際の実装では避けるべきですが、ここでは後方互換性のため）
+	var fizzBuzzType int
+	switch typeImpl.(type) {
+	case FizzBuzzType01:
+		fizzBuzzType = 1
+	case FizzBuzzType02:
+		fizzBuzzType = 2
+	case FizzBuzzType03:
+		fizzBuzzType = 3
+	default:
+		panic("該当するタイプは存在しません")
+	}
+	
+	return &FizzBuzz{
+		list:        make([]string, 0),
+		fizzBuzzType: fizzBuzzType,
+		typeImpl:    typeImpl,
+	}
+}
+
 // Generate 数を文字列に変換してFizzBuzzルールを適用
 func (f *FizzBuzz) Generate(number int) string {
 	return f.typeImpl.Generate(number)
diff --git a/app/go-tdd-tutorial b/app/go-tdd-tutorial
index 12ef392..fa9b01a 100644
Binary files a/app/go-tdd-tutorial and b/app/go-tdd-tutorial differ
diff --git a/app/main_test.go b/app/main_test.go
index c4b3a87..fc60098 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -114,3 +114,31 @@ func TestGenerateByType_それ以外のタイプで例外が発生する(t *test
 	fizzbuzz := NewFizzBuzz(4)
 	fizzbuzz.Generate(1)
 }
+
+// 値オブジェクトのテスト
+func Test値オブジェクトを使用したFizzBuzz_タイプ1(t *testing.T) {
+	typeInstance := FizzBuzzType01{}
+	fizzbuzz := NewFizzBuzzWithType(typeInstance)
+	got := fizzbuzz.Generate(3)
+	if got != "Fizz" {
+		t.Errorf("FizzBuzz with value object type = %v, want Fizz", got)
+	}
+}
+
+func Test値オブジェクトを使用したFizzBuzz_タイプ2(t *testing.T) {
+	typeInstance := FizzBuzzType02{}
+	fizzbuzz := NewFizzBuzzWithType(typeInstance)
+	got := fizzbuzz.Generate(3)
+	if got != "3" {
+		t.Errorf("FizzBuzz with value object type = %v, want 3", got)
+	}
+}
+
+func Test値オブジェクトを使用したFizzBuzz_タイプ3(t *testing.T) {
+	typeInstance := FizzBuzzType03{}
+	fizzbuzz := NewFizzBuzzWithType(typeInstance)
+	got := fizzbuzz.Generate(15)
+	if got != "FizzBuzz" {
+		t.Errorf("FizzBuzz with value object type = %v, want FizzBuzz", got)
+	}
+}

```

## コミット: d2243ad

### メッセージ

```
refactor: メソッドの移動
```

### 変更されたファイル

- M	app/fizzbuzz.go

### 変更内容

```diff
commit d2243adecbb737b26897efe346441016c00ad3bb
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:16:31 2025 +0000

    refactor: メソッドの移動

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 531c131..12964bc 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -13,6 +13,20 @@ type FizzBuzzType interface {
 // FizzBuzzTypeBase 共通処理を提供する基底構造体
 type FizzBuzzTypeBase struct{}
 
+// Create ファクトリメソッド
+func (f FizzBuzzTypeBase) Create(fizzBuzzType int) FizzBuzzType {
+	switch fizzBuzzType {
+	case 1:
+		return FizzBuzzType01{}
+	case 2:
+		return FizzBuzzType02{}
+	case 3:
+		return FizzBuzzType03{}
+	default:
+		panic("該当するタイプは存在しません")
+	}
+}
+
 func (f FizzBuzzTypeBase) Fizz(number int) bool {
 	return number%3 == 0
 }
@@ -75,18 +89,8 @@ type FizzBuzz struct {
 
 // NewFizzBuzz コンストラクタ
 func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
-	var typeImpl FizzBuzzType
-	
-	switch fizzBuzzType {
-	case 1:
-		typeImpl = FizzBuzzType01{}
-	case 2:
-		typeImpl = FizzBuzzType02{}
-	case 3:
-		typeImpl = FizzBuzzType03{}
-	default:
-		panic("該当するタイプは存在しません")
-	}
+	base := FizzBuzzTypeBase{}
+	typeImpl := base.Create(fizzBuzzType)
 	
 	return &FizzBuzz{
 		list:        make([]string, 0),

```

## コミット: d692923

### メッセージ

```
refactor: メソッド名の変更
```

### 変更されたファイル

- M	app/fizzbuzz.go

### 変更内容

```diff
commit d692923cde2f7c9e13218af9491b008c307d2c7a
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:14:58 2025 +0000

    refactor: メソッド名の変更

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index acf8178..531c131 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -13,11 +13,11 @@ type FizzBuzzType interface {
 // FizzBuzzTypeBase 共通処理を提供する基底構造体
 type FizzBuzzTypeBase struct{}
 
-func (f FizzBuzzTypeBase) IsFizz(number int) bool {
+func (f FizzBuzzTypeBase) Fizz(number int) bool {
 	return number%3 == 0
 }
 
-func (f FizzBuzzTypeBase) IsBuzz(number int) bool {
+func (f FizzBuzzTypeBase) Buzz(number int) bool {
 	return number%5 == 0
 }
 
@@ -27,8 +27,8 @@ type FizzBuzzType01 struct {
 }
 
 func (f FizzBuzzType01) Generate(number int) string {
-	isFizz := f.IsFizz(number)
-	isBuzz := f.IsBuzz(number)
+	isFizz := f.Fizz(number)
+	isBuzz := f.Buzz(number)
 
 	if isFizz && isBuzz {
 		return "FizzBuzz"
@@ -57,8 +57,8 @@ type FizzBuzzType03 struct {
 }
 
 func (f FizzBuzzType03) Generate(number int) string {
-	isFizz := f.IsFizz(number)
-	isBuzz := f.IsBuzz(number)
+	isFizz := f.Fizz(number)
+	isBuzz := f.Buzz(number)
 
 	if isFizz && isBuzz {
 		return "FizzBuzz"

```

## コミット: 9c1bd3c

### メッセージ

```
refactor: スーパークラスの抽出
```

### 変更されたファイル

- M	app/fizzbuzz.go

### 変更内容

```diff
commit 9c1bd3c8e15851ca015e86fd50cb517b4cb062aa
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:13:17 2025 +0000

    refactor: スーパークラスの抽出

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 9943c53..acf8178 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -10,12 +10,25 @@ type FizzBuzzType interface {
 	Generate(number int) string
 }
 
+// FizzBuzzTypeBase 共通処理を提供する基底構造体
+type FizzBuzzTypeBase struct{}
+
+func (f FizzBuzzTypeBase) IsFizz(number int) bool {
+	return number%3 == 0
+}
+
+func (f FizzBuzzTypeBase) IsBuzz(number int) bool {
+	return number%5 == 0
+}
+
 // FizzBuzzType01 タイプ1の実装
-type FizzBuzzType01 struct{}
+type FizzBuzzType01 struct {
+	FizzBuzzTypeBase
+}
 
 func (f FizzBuzzType01) Generate(number int) string {
-	isFizz := number%3 == 0
-	isBuzz := number%5 == 0
+	isFizz := f.IsFizz(number)
+	isBuzz := f.IsBuzz(number)
 
 	if isFizz && isBuzz {
 		return "FizzBuzz"
@@ -30,18 +43,22 @@ func (f FizzBuzzType01) Generate(number int) string {
 }
 
 // FizzBuzzType02 タイプ2の実装
-type FizzBuzzType02 struct{}
+type FizzBuzzType02 struct {
+	FizzBuzzTypeBase
+}
 
 func (f FizzBuzzType02) Generate(number int) string {
 	return strconv.Itoa(number)
 }
 
 // FizzBuzzType03 タイプ3の実装
-type FizzBuzzType03 struct{}
+type FizzBuzzType03 struct {
+	FizzBuzzTypeBase
+}
 
 func (f FizzBuzzType03) Generate(number int) string {
-	isFizz := number%3 == 0
-	isBuzz := number%5 == 0
+	isFizz := f.IsFizz(number)
+	isBuzz := f.IsBuzz(number)
 
 	if isFizz && isBuzz {
 		return "FizzBuzz"

```

## コミット: ae78097

### メッセージ

```
refactor: ポリモーフィズムによる条件記述の置き換え
```

### 変更されたファイル

- M	app/fizzbuzz.go

### 変更内容

```diff
commit ae780976f5b65de09dd29160a9d053c6cc8f465f
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:06:50 2025 +0000

    refactor: ポリモーフィズムによる条件記述の置き換え

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 6b4b5a7..9943c53 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -5,47 +5,82 @@ import (
 	"strconv"
 )
 
-// FizzBuzz構造体
-type FizzBuzz struct {
-	list        []string // FizzBuzz配列を保持するフィールド
-	fizzBuzzType int     // FizzBuzzのタイプを保持するフィールド
+// FizzBuzzType インターフェース
+type FizzBuzzType interface {
+	Generate(number int) string
 }
 
-// NewFizzBuzz コンストラクタ
-func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
-	return &FizzBuzz{
-		list:        make([]string, 0),
-		fizzBuzzType: fizzBuzzType,
+// FizzBuzzType01 タイプ1の実装
+type FizzBuzzType01 struct{}
+
+func (f FizzBuzzType01) Generate(number int) string {
+	isFizz := number%3 == 0
+	isBuzz := number%5 == 0
+
+	if isFizz && isBuzz {
+		return "FizzBuzz"
+	}
+	if isFizz {
+		return "Fizz"
 	}
+	if isBuzz {
+		return "Buzz"
+	}
+	return strconv.Itoa(number)
 }
 
-// Generate 数を文字列に変換してFizzBuzzルールを適用
-func (f *FizzBuzz) Generate(number int) string {
+// FizzBuzzType02 タイプ2の実装
+type FizzBuzzType02 struct{}
+
+func (f FizzBuzzType02) Generate(number int) string {
+	return strconv.Itoa(number)
+}
+
+// FizzBuzzType03 タイプ3の実装
+type FizzBuzzType03 struct{}
+
+func (f FizzBuzzType03) Generate(number int) string {
 	isFizz := number%3 == 0
 	isBuzz := number%5 == 0
 
-	switch f.fizzBuzzType {
+	if isFizz && isBuzz {
+		return "FizzBuzz"
+	}
+	return strconv.Itoa(number)
+}
+
+// FizzBuzz構造体
+type FizzBuzz struct {
+	list        []string      // FizzBuzz配列を保持するフィールド
+	fizzBuzzType int          // FizzBuzzのタイプを保持するフィールド
+	typeImpl    FizzBuzzType  // タイプ実装
+}
+
+// NewFizzBuzz コンストラクタ
+func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
+	var typeImpl FizzBuzzType
+	
+	switch fizzBuzzType {
 	case 1:
-		if isFizz && isBuzz {
-			return "FizzBuzz"
-		}
-		if isFizz {
-			return "Fizz"
-		}
-		if isBuzz {
-			return "Buzz"
-		}
-		return strconv.Itoa(number)
+		typeImpl = FizzBuzzType01{}
 	case 2:
-		return strconv.Itoa(number)
+		typeImpl = FizzBuzzType02{}
 	case 3:
-		if isFizz && isBuzz {
-			return "FizzBuzz"
-		}
-		return strconv.Itoa(number)
+		typeImpl = FizzBuzzType03{}
 	default:
 		panic("該当するタイプは存在しません")
 	}
+	
+	return &FizzBuzz{
+		list:        make([]string, 0),
+		fizzBuzzType: fizzBuzzType,
+		typeImpl:    typeImpl,
+	}
+}
+
+// Generate 数を文字列に変換してFizzBuzzルールを適用
+func (f *FizzBuzz) Generate(number int) string {
+	return f.typeImpl.Generate(number)
 }
 
 // Type FizzBuzzタイプを取得

```

## コミット: 4c19be8

### メッセージ

```
refactor: フィールドのカプセル化
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/go-tdd-tutorial

### 変更内容

```diff
commit 4c19be88c1f4759d50d8c86b7f702303065bd5d2
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 06:04:50 2025 +0000

    refactor: フィールドのカプセル化

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 3d44c8d..6b4b5a7 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -48,6 +48,11 @@ func (f *FizzBuzz) Generate(number int) string {
 	}
 }
 
+// Type FizzBuzzタイプを取得
+func (f *FizzBuzz) Type() int {
+	return f.fizzBuzzType
+}
+
 // List FizzBuzz配列を取得
 func (f *FizzBuzz) List() []string {
 	return f.list
diff --git a/app/go-tdd-tutorial b/app/go-tdd-tutorial
index 7007d7b..12ef392 100644
Binary files a/app/go-tdd-tutorial and b/app/go-tdd-tutorial differ

```

## コミット: 3d5c094

### メッセージ

```
refactor: ステートメントのスライド
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/main_test.go

### 変更内容

```diff
commit 3d5c094c6a4e2075e76e863d5c61aa25a970b157
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:57:00 2025 +0000

    refactor: ステートメントのスライド

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index 6016332..3d44c8d 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -7,34 +7,39 @@ import (
 
 // FizzBuzz構造体
 type FizzBuzz struct {
-	list []string // FizzBuzz配列を保持するフィールド
+	list        []string // FizzBuzz配列を保持するフィールド
+	fizzBuzzType int     // FizzBuzzのタイプを保持するフィールド
 }
 
 // NewFizzBuzz コンストラクタ
-func NewFizzBuzz() *FizzBuzz {
+func NewFizzBuzz(fizzBuzzType int) *FizzBuzz {
 	return &FizzBuzz{
-		list: make([]string, 0),
+		list:        make([]string, 0),
+		fizzBuzzType: fizzBuzzType,
 	}
 }
 
 // Generate 数を文字列に変換してFizzBuzzルールを適用
-func (f *FizzBuzz) Generate(number, fizzBuzzType int) string {
-	switch fizzBuzzType {
+func (f *FizzBuzz) Generate(number int) string {
+	isFizz := number%3 == 0
+	isBuzz := number%5 == 0
+
+	switch f.fizzBuzzType {
 	case 1:
-		if number%3 == 0 && number%5 == 0 {
+		if isFizz && isBuzz {
 			return "FizzBuzz"
 		}
-		if number%3 == 0 {
+		if isFizz {
 			return "Fizz"
 		}
-		if number%5 == 0 {
+		if isBuzz {
 			return "Buzz"
 		}
 		return strconv.Itoa(number)
 	case 2:
 		return strconv.Itoa(number)
 	case 3:
-		if number%3 == 0 && number%5 == 0 {
+		if isFizz && isBuzz {
 			return "FizzBuzz"
 		}
 		return strconv.Itoa(number)
@@ -52,30 +57,30 @@ func (f *FizzBuzz) List() []string {
 func (f *FizzBuzz) GenerateList(start, end int) {
 	f.list = make([]string, 0, end-start+1)
 	for i := start; i <= end; i++ {
-		f.list = append(f.list, f.Generate(i, 1)) // デフォルトはタイプ1
+		f.list = append(f.list, f.Generate(i))
 	}
 }
 
 // 後方互換性のためのラッパー関数
 func Generate(number int) string {
-	fizzbuzz := NewFizzBuzz()
-	return fizzbuzz.Generate(number, 1)
+	fizzbuzz := NewFizzBuzz(1)
+	return fizzbuzz.Generate(number)
 }
 
 func GenerateByType(number, fizzBuzzType int) string {
-	fizzbuzz := NewFizzBuzz()
-	return fizzbuzz.Generate(number, fizzBuzzType)
+	fizzbuzz := NewFizzBuzz(fizzBuzzType)
+	return fizzbuzz.Generate(number)
 }
 
 func GenerateList(start, end int) []string {
-	fizzbuzz := NewFizzBuzz()
+	fizzbuzz := NewFizzBuzz(1)
 	fizzbuzz.GenerateList(start, end)
 	return fizzbuzz.List()
 }
 
 func main() {
 	fmt.Println("FizzBuzz Game:")
-	fizzbuzz := NewFizzBuzz()
+	fizzbuzz := NewFizzBuzz(1)
 	fizzbuzz.GenerateList(1, 100)
 	results := fizzbuzz.List()
 	for _, result := range results {
diff --git a/app/main_test.go b/app/main_test.go
index f110d10..c4b3a87 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -4,10 +4,10 @@ import "testing"
 
 func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected string) {
 	t.Helper()
-	fizzbuzz := NewFizzBuzz()
-	got := fizzbuzz.Generate(number, fizzBuzzType)
+	fizzbuzz := NewFizzBuzz(fizzBuzzType)
+	got := fizzbuzz.Generate(number)
 	if got != expected {
-		t.Errorf("FizzBuzz.Generate(%d, %d) = %v, want %v", number, fizzBuzzType, got, expected)
+		t.Errorf("FizzBuzz.Generate(%d) with type %d = %v, want %v", number, fizzBuzzType, got, expected)
 	}
 }
 
@@ -36,7 +36,7 @@ func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
 
 // 1から100までのFizzBuzzの配列を返す
 func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
-	fizzbuzz := NewFizzBuzz()
+	fizzbuzz := NewFizzBuzz(1)
 	fizzbuzz.GenerateList(1, 100)
 	results := fizzbuzz.List()
 
@@ -108,9 +108,9 @@ func Test15を渡したら文字列FizzBuzzを返す_タイプ3(t *testing.T) {
 func TestGenerateByType_それ以外のタイプで例外が発生する(t *testing.T) {
 	defer func() {
 		if r := recover(); r == nil {
-			t.Errorf("FizzBuzz.Generate(1, 4) should panic")
+			t.Errorf("FizzBuzz.Generate(1) with type 4 should panic")
 		}
 	}()
-	fizzbuzz := NewFizzBuzz()
-	fizzbuzz.Generate(1, 4)
+	fizzbuzz := NewFizzBuzz(4)
+	fizzbuzz.Generate(1)
 }

```

## コミット: 0f26bd4

### メッセージ

```
refactor: フィールドのカプセル化
```

### 変更されたファイル

- M	app/fizzbuzz.go
- M	app/main_test.go

### 変更内容

```diff
commit 0f26bd441f6ffe2c55c3cf073971c67bd8293b67
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:52:24 2025 +0000

    refactor: フィールドのカプセル化

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
index e416a19..6016332 100644
--- a/app/fizzbuzz.go
+++ b/app/fizzbuzz.go
@@ -6,11 +6,15 @@ import (
 )
 
 // FizzBuzz構造体
-type FizzBuzz struct{}
+type FizzBuzz struct {
+	list []string // FizzBuzz配列を保持するフィールド
+}
 
 // NewFizzBuzz コンストラクタ
 func NewFizzBuzz() *FizzBuzz {
-	return &FizzBuzz{}
+	return &FizzBuzz{
+		list: make([]string, 0),
+	}
 }
 
 // Generate 数を文字列に変換してFizzBuzzルールを適用
@@ -39,13 +43,17 @@ func (f *FizzBuzz) Generate(number, fizzBuzzType int) string {
 	}
 }
 
-// GenerateList 範囲指定してFizzBuzzのリストを作成
-func (f *FizzBuzz) GenerateList(start, end int) []string {
-	results := make([]string, 0, end-start+1)
+// List FizzBuzz配列を取得
+func (f *FizzBuzz) List() []string {
+	return f.list
+}
+
+// GenerateList 範囲指定してFizzBuzzのリストを作成し、インスタンス変数に保存
+func (f *FizzBuzz) GenerateList(start, end int) {
+	f.list = make([]string, 0, end-start+1)
 	for i := start; i <= end; i++ {
-		results = append(results, f.Generate(i, 1)) // デフォルトはタイプ1
+		f.list = append(f.list, f.Generate(i, 1)) // デフォルトはタイプ1
 	}
-	return results
 }
 
 // 後方互換性のためのラッパー関数
@@ -61,13 +69,15 @@ func GenerateByType(number, fizzBuzzType int) string {
 
 func GenerateList(start, end int) []string {
 	fizzbuzz := NewFizzBuzz()
-	return fizzbuzz.GenerateList(start, end)
+	fizzbuzz.GenerateList(start, end)
+	return fizzbuzz.List()
 }
 
 func main() {
 	fmt.Println("FizzBuzz Game:")
 	fizzbuzz := NewFizzBuzz()
-	results := fizzbuzz.GenerateList(1, 100)
+	fizzbuzz.GenerateList(1, 100)
+	results := fizzbuzz.List()
 	for _, result := range results {
 		fmt.Println(result)
 	}
diff --git a/app/main_test.go b/app/main_test.go
index 4153e40..f110d10 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -37,7 +37,8 @@ func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
 // 1から100までのFizzBuzzの配列を返す
 func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
 	fizzbuzz := NewFizzBuzz()
-	results := fizzbuzz.GenerateList(1, 100)
+	fizzbuzz.GenerateList(1, 100)
+	results := fizzbuzz.List()
 
 	// いくつかの値を確認
 	if len(results) != 100 {

```

## コミット: 44eb232

### メッセージ

```
refactor: 関数からオブジェクト指向の構造体とメソッドに移行
```

### 変更されたファイル

- A	app/fizzbuzz.go
- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 44eb2324b01551219aca0de9ee1c92dc736c5c12
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:44:34 2025 +0000

    refactor: 関数からオブジェクト指向の構造体とメソッドに移行

diff --git a/app/fizzbuzz.go b/app/fizzbuzz.go
new file mode 100644
index 0000000..e416a19
--- /dev/null
+++ b/app/fizzbuzz.go
@@ -0,0 +1,74 @@
+package main
+
+import (
+	"fmt"
+	"strconv"
+)
+
+// FizzBuzz構造体
+type FizzBuzz struct{}
+
+// NewFizzBuzz コンストラクタ
+func NewFizzBuzz() *FizzBuzz {
+	return &FizzBuzz{}
+}
+
+// Generate 数を文字列に変換してFizzBuzzルールを適用
+func (f *FizzBuzz) Generate(number, fizzBuzzType int) string {
+	switch fizzBuzzType {
+	case 1:
+		if number%3 == 0 && number%5 == 0 {
+			return "FizzBuzz"
+		}
+		if number%3 == 0 {
+			return "Fizz"
+		}
+		if number%5 == 0 {
+			return "Buzz"
+		}
+		return strconv.Itoa(number)
+	case 2:
+		return strconv.Itoa(number)
+	case 3:
+		if number%3 == 0 && number%5 == 0 {
+			return "FizzBuzz"
+		}
+		return strconv.Itoa(number)
+	default:
+		panic("該当するタイプは存在しません")
+	}
+}
+
+// GenerateList 範囲指定してFizzBuzzのリストを作成
+func (f *FizzBuzz) GenerateList(start, end int) []string {
+	results := make([]string, 0, end-start+1)
+	for i := start; i <= end; i++ {
+		results = append(results, f.Generate(i, 1)) // デフォルトはタイプ1
+	}
+	return results
+}
+
+// 後方互換性のためのラッパー関数
+func Generate(number int) string {
+	fizzbuzz := NewFizzBuzz()
+	return fizzbuzz.Generate(number, 1)
+}
+
+func GenerateByType(number, fizzBuzzType int) string {
+	fizzbuzz := NewFizzBuzz()
+	return fizzbuzz.Generate(number, fizzBuzzType)
+}
+
+func GenerateList(start, end int) []string {
+	fizzbuzz := NewFizzBuzz()
+	return fizzbuzz.GenerateList(start, end)
+}
+
+func main() {
+	fmt.Println("FizzBuzz Game:")
+	fizzbuzz := NewFizzBuzz()
+	results := fizzbuzz.GenerateList(1, 100)
+	for _, result := range results {
+		fmt.Println(result)
+	}
+}
diff --git a/app/main.go b/app/main.go
index cee2e02..06ab7d0 100644
--- a/app/main.go
+++ b/app/main.go
@@ -1,60 +1 @@
 package main
-
-import (
-    "fmt"
-    "strconv"
-)
-
-func Generate(number int) string {
-    if number%3 == 0 && number%5 == 0 {
-        return "FizzBuzz"
-    }
-    if number%3 == 0 {
-        return "Fizz"
-    }
-    if number%5 == 0 {
-        return "Buzz"
-    }
-    return strconv.Itoa(number)
-}
-
-func GenerateList(start, end int) []string {
-    results := make([]string, 0, end-start+1)
-    for i := start; i <= end; i++ {
-        results = append(results, Generate(i))
-    }
-    return results
-}
-
-func GenerateByType(number, fizzBuzzType int) string {
-    switch fizzBuzzType {
-    case 1:
-        if number%3 == 0 && number%5 == 0 {
-            return "FizzBuzz"
-        }
-        if number%3 == 0 {
-            return "Fizz"
-        }
-        if number%5 == 0 {
-            return "Buzz"
-        }
-        return strconv.Itoa(number)
-    case 2:
-        return strconv.Itoa(number)
-    case 3:
-        if number%3 == 0 && number%5 == 0 {
-            return "FizzBuzz"
-        }
-        return strconv.Itoa(number)
-    default:
-        panic("未定義のタイプです")
-    }
-}
-
-func main() {
-    fmt.Println("FizzBuzz Game:")
-    results := GenerateList(1, 100)
-    for _, result := range results {
-        fmt.Println(result)
-    }
-}
diff --git a/app/main_test.go b/app/main_test.go
index a9f6539..4153e40 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -4,9 +4,10 @@ import "testing"
 
 func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected string) {
 	t.Helper()
-	got := GenerateByType(number, fizzBuzzType)
+	fizzbuzz := NewFizzBuzz()
+	got := fizzbuzz.Generate(number, fizzBuzzType)
 	if got != expected {
-		t.Errorf("GenerateByType(%d, %d) = %v, want %v", number, fizzBuzzType, got, expected)
+		t.Errorf("FizzBuzz.Generate(%d, %d) = %v, want %v", number, fizzBuzzType, got, expected)
 	}
 }
 
@@ -35,7 +36,8 @@ func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
 
 // 1から100までのFizzBuzzの配列を返す
 func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
-	results := GenerateList(1, 100)
+	fizzbuzz := NewFizzBuzz()
+	results := fizzbuzz.GenerateList(1, 100)
 
 	// いくつかの値を確認
 	if len(results) != 100 {
@@ -105,8 +107,9 @@ func Test15を渡したら文字列FizzBuzzを返す_タイプ3(t *testing.T) {
 func TestGenerateByType_それ以外のタイプで例外が発生する(t *testing.T) {
 	defer func() {
 		if r := recover(); r == nil {
-			t.Errorf("GenerateByType(1, 4) should panic")
+			t.Errorf("FizzBuzz.Generate(1, 4) should panic")
 		}
 	}()
-	GenerateByType(1, 4)
+	fizzbuzz := NewFizzBuzz()
+	fizzbuzz.Generate(1, 4)
 }

```

## コミット: 976b0b1

### メッセージ

```
feat: それ以外のタイプの場合の例外処理を追加
```

### 変更されたファイル

- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 976b0b139f1dd2129fad8c6afff386b8589d237b
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:42:00 2025 +0000

    feat: それ以外のタイプの場合の例外処理を追加

diff --git a/app/main.go b/app/main.go
index 9b5e2ad..cee2e02 100644
--- a/app/main.go
+++ b/app/main.go
@@ -47,7 +47,7 @@ func GenerateByType(number, fizzBuzzType int) string {
         }
         return strconv.Itoa(number)
     default:
-        return ""
+        panic("未定義のタイプです")
     }
 }
 
diff --git a/app/main_test.go b/app/main_test.go
index 01a879d..a9f6539 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -100,3 +100,13 @@ func Test5を渡したら文字列5を返す_タイプ3(t *testing.T) {
 func Test15を渡したら文字列FizzBuzzを返す_タイプ3(t *testing.T) {
 	assertGenerateByType(t, 15, 3, "FizzBuzz")
 }
+
+// それ以外のタイプの場合
+func TestGenerateByType_それ以外のタイプで例外が発生する(t *testing.T) {
+	defer func() {
+		if r := recover(); r == nil {
+			t.Errorf("GenerateByType(1, 4) should panic")
+		}
+	}()
+	GenerateByType(1, 4)
+}

```

## コミット: 7dc1c2d

### メッセージ

```
feat: タイプ2とタイプ3の場合を追加
```

### 変更されたファイル

- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 7dc1c2d87ce1bff39c8b860822bdf2d1a8514bba
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:37:45 2025 +0000

    feat: タイプ2とタイプ3の場合を追加

diff --git a/app/main.go b/app/main.go
index 91500b7..9b5e2ad 100644
--- a/app/main.go
+++ b/app/main.go
@@ -39,6 +39,13 @@ func GenerateByType(number, fizzBuzzType int) string {
             return "Buzz"
         }
         return strconv.Itoa(number)
+    case 2:
+        return strconv.Itoa(number)
+    case 3:
+        if number%3 == 0 && number%5 == 0 {
+            return "FizzBuzz"
+        }
+        return strconv.Itoa(number)
     default:
         return ""
     }
diff --git a/app/main_test.go b/app/main_test.go
index 0a715c6..01a879d 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -56,3 +56,47 @@ func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
 		t.Errorf("results[14] = %s, want FizzBuzz", results[14])
 	}
 }
+
+// タイプ2の場合
+
+// その他の場合
+func Test1を渡したら文字列1を返す_タイプ2(t *testing.T) {
+	assertGenerateByType(t, 1, 2, "1")
+}
+
+// 3の倍数のときは数を文字列にして返す
+func Test3を渡したら文字列3を返す_タイプ2(t *testing.T) {
+	assertGenerateByType(t, 3, 2, "3")
+}
+
+// 5の倍数のときは数を文字列にして返す
+func Test5を渡したら文字列5を返す_タイプ2(t *testing.T) {
+	assertGenerateByType(t, 5, 2, "5")
+}
+
+// 3と5両方の倍数の場合には数を文字列にして返す
+func Test15を渡したら文字列15を返す_タイプ2(t *testing.T) {
+	assertGenerateByType(t, 15, 2, "15")
+}
+
+// タイプ3の場合
+
+// その他の場合
+func Test1を渡したら文字列1を返す_タイプ3(t *testing.T) {
+	assertGenerateByType(t, 1, 3, "1")
+}
+
+// 3の倍数のときは数を文字列にして返す
+func Test3を渡したら文字列3を返す_タイプ3(t *testing.T) {
+	assertGenerateByType(t, 3, 3, "3")
+}
+
+// 5の倍数のときは数を文字列にして返す
+func Test5を渡したら文字列5を返す_タイプ3(t *testing.T) {
+	assertGenerateByType(t, 5, 3, "5")
+}
+
+// 3と5両方の倍数の場合にはFizzBuzzと返す
+func Test15を渡したら文字列FizzBuzzを返す_タイプ3(t *testing.T) {
+	assertGenerateByType(t, 15, 3, "FizzBuzz")
+}

```

## コミット: 23d5d5f

### メッセージ

```
refactor: メソッドのインライン化
```

### 変更されたファイル

- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 23d5d5fda893758c02aa2ffce3c55f1c3b2ea193
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:32:44 2025 +0000

    refactor: メソッドのインライン化

diff --git a/app/main.go b/app/main.go
index 50d2ceb..91500b7 100644
--- a/app/main.go
+++ b/app/main.go
@@ -27,7 +27,21 @@ func GenerateList(start, end int) []string {
 }
 
 func GenerateByType(number, fizzBuzzType int) string {
-    return "1"
+    switch fizzBuzzType {
+    case 1:
+        if number%3 == 0 && number%5 == 0 {
+            return "FizzBuzz"
+        }
+        if number%3 == 0 {
+            return "Fizz"
+        }
+        if number%5 == 0 {
+            return "Buzz"
+        }
+        return strconv.Itoa(number)
+    default:
+        return ""
+    }
 }
 
 func main() {
diff --git a/app/main_test.go b/app/main_test.go
index f59247b..0a715c6 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -2,43 +2,39 @@ package main
 
 import "testing"
 
-func assertGenerate(t *testing.T, input int, expected string) {
+func assertGenerateByType(t *testing.T, number, fizzBuzzType int, expected string) {
 	t.Helper()
-	got := Generate(input)
+	got := GenerateByType(number, fizzBuzzType)
 	if got != expected {
-		t.Errorf("Generate(%d) = %v, want %v", input, got, expected)
+		t.Errorf("GenerateByType(%d, %d) = %v, want %v", number, fizzBuzzType, got, expected)
 	}
 }
 
-func Test1を渡したら文字列1を返す(t *testing.T) {
-	assertGenerate(t, 1, "1")
-}
-
-func Test2を渡したら文字列2を返す(t *testing.T) {
-	assertGenerate(t, 2, "2")
-}
-
-func Test3を渡したらFizzを返す(t *testing.T) {
-	assertGenerate(t, 3, "Fizz")
-}
+// 数を文字列にして返す
+// タイプ1の場合
 
-func Test6を渡したらFizzを返す(t *testing.T) {
-	assertGenerate(t, 6, "Fizz")
+// 三の倍数の場合
+func Test3を渡したら文字列Fizzを返す_タイプ1(t *testing.T) {
+	assertGenerateByType(t, 3, 1, "Fizz")
 }
 
-func Test5を渡したらBuzzを返す(t *testing.T) {
-	assertGenerate(t, 5, "Buzz")
+// 五の倍数の場合
+func Test5を渡したら文字列Buzzを返す_タイプ1(t *testing.T) {
+	assertGenerateByType(t, 5, 1, "Buzz")
 }
 
-func Test10を渡したらBuzzを返す(t *testing.T) {
-	assertGenerate(t, 10, "Buzz")
+// 三と五の倍数の場合
+func Test15を渡したら文字列FizzBuzzを返す_タイプ1(t *testing.T) {
+	assertGenerateByType(t, 15, 1, "FizzBuzz")
 }
 
-func Test15を渡したらFizzBuzzを返す(t *testing.T) {
-	assertGenerate(t, 15, "FizzBuzz")
+// その他の場合
+func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
+	assertGenerateByType(t, 1, 1, "1")
 }
 
-func Test1から100までのFizzBuzzを返す(t *testing.T) {
+// 1から100までのFizzBuzzの配列を返す
+func Test1から100までのFizzBuzzを返す_タイプ1(t *testing.T) {
 	results := GenerateList(1, 100)
 
 	// いくつかの値を確認
@@ -60,13 +56,3 @@ func Test1から100までのFizzBuzzを返す(t *testing.T) {
 		t.Errorf("results[14] = %s, want FizzBuzz", results[14])
 	}
 }
-
-// タイプごとに出力を切り替えることができる
-// タイプ1の場合
-func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
-	got := GenerateByType(1, 1)
-	expected := "1"
-	if got != expected {
-		t.Errorf("GenerateByType(1, 1) = %v, want %v", got, expected)
-	}
-}

```

## コミット: 1f9e50f

### メッセージ

```
feat: タイプ1の基本機能を追加
```

### 変更されたファイル

- A	app/go-tdd-tutorial
- M	app/main.go
- M	app/main_test.go
- A	docs/wiki

### 変更内容

```diff
commit 1f9e50fc535de94bd28e298bf7dd4ace7ffdb937
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:31:00 2025 +0000

    feat: タイプ1の基本機能を追加

diff --git a/app/go-tdd-tutorial b/app/go-tdd-tutorial
new file mode 100644
index 0000000..7007d7b
Binary files /dev/null and b/app/go-tdd-tutorial differ
diff --git a/app/main.go b/app/main.go
index bb7027d..50d2ceb 100644
--- a/app/main.go
+++ b/app/main.go
@@ -1,35 +1,39 @@
 package main
 
 import (
-	"fmt"
-	"strconv"
+    "fmt"
+    "strconv"
 )
 
 func Generate(number int) string {
-	if number%3 == 0 && number%5 == 0 {
-		return "FizzBuzz"
-	}
-	if number%3 == 0 {
-		return "Fizz"
-	}
-	if number%5 == 0 {
-		return "Buzz"
-	}
-	return strconv.Itoa(number)
+    if number%3 == 0 && number%5 == 0 {
+        return "FizzBuzz"
+    }
+    if number%3 == 0 {
+        return "Fizz"
+    }
+    if number%5 == 0 {
+        return "Buzz"
+    }
+    return strconv.Itoa(number)
 }
 
 func GenerateList(start, end int) []string {
-	results := make([]string, 0, end-start+1)
-	for i := start; i <= end; i++ {
-		results = append(results, Generate(i))
-	}
-	return results
+    results := make([]string, 0, end-start+1)
+    for i := start; i <= end; i++ {
+        results = append(results, Generate(i))
+    }
+    return results
+}
+
+func GenerateByType(number, fizzBuzzType int) string {
+    return "1"
 }
 
 func main() {
-	fmt.Println("FizzBuzz Game:")
-	results := GenerateList(1, 100)
-	for _, result := range results {
-		fmt.Println(result)
-	}
+    fmt.Println("FizzBuzz Game:")
+    results := GenerateList(1, 100)
+    for _, result := range results {
+        fmt.Println(result)
+    }
 }
diff --git a/app/main_test.go b/app/main_test.go
index 0b2d3dd..f59247b 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -60,3 +60,13 @@ func Test1から100までのFizzBuzzを返す(t *testing.T) {
 		t.Errorf("results[14] = %s, want FizzBuzz", results[14])
 	}
 }
+
+// タイプごとに出力を切り替えることができる
+// タイプ1の場合
+func Test1を渡したら文字列1を返す_タイプ1(t *testing.T) {
+	got := GenerateByType(1, 1)
+	expected := "1"
+	if got != expected {
+		t.Errorf("GenerateByType(1, 1) = %v, want %v", got, expected)
+	}
+}
diff --git a/docs/wiki b/docs/wiki
new file mode 160000
index 0000000..91233c4
--- /dev/null
+++ b/docs/wiki
@@ -0,0 +1 @@
+Subproject commit 91233c4e152502582a93e86462b8963a0c2a5b35

```

## コミット: 103cdbb

### メッセージ

```
docs: wikiサブプロジェクトを削除
```

### 変更されたファイル

- D	docs/wiki

### 変更内容

```diff
commit 103cdbb5774a8f1dad1768e1326238eb69d9c87f
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:22:44 2025 +0000

    docs: wikiサブプロジェクトを削除

diff --git a/docs/wiki b/docs/wiki
deleted file mode 160000
index 91233c4..0000000
--- a/docs/wiki
+++ /dev/null
@@ -1 +0,0 @@
-Subproject commit 91233c4e152502582a93e86462b8963a0c2a5b35

```

## コミット: 95aeceb

### メッセージ

```
docs: make→taskの残りの修正を完了
```

### 変更されたファイル

- A	docs/wiki

### 変更内容

```diff
commit 95aeceb3f8f6443e631acfdcffd6b0a038df3b65
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:15:57 2025 +0000

    docs: make→taskの残りの修正を完了

diff --git a/docs/wiki b/docs/wiki
new file mode 160000
index 0000000..91233c4
--- /dev/null
+++ b/docs/wiki
@@ -0,0 +1 @@
+Subproject commit 91233c4e152502582a93e86462b8963a0c2a5b35

```

## コミット: bc9806a

### メッセージ

```
chore: タスクランナーセットアップ
```

### 変更されたファイル

- A	app/.githooks/pre-commit
- A	app/Taskfile.yml
- A	app/setup-hooks.sh

### 変更内容

```diff
commit bc9806aa4d55c425d6cbed2bfea85e4fd5d41f10
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 05:03:50 2025 +0000

    chore: タスクランナーセットアップ

diff --git a/app/.githooks/pre-commit b/app/.githooks/pre-commit
new file mode 100644
index 0000000..c397ef4
--- /dev/null
+++ b/app/.githooks/pre-commit
@@ -0,0 +1,33 @@
+#!/bin/sh
+# pre-commit hook for Go TDD Tutorial
+# コミット前に品質チェックを自動実行
+
+echo "pre-commit hook: 品質チェックを実行中..."
+
+# go-taskのパスを設定
+export PATH=$PATH:$(go env GOPATH)/bin
+
+# コードフォーマットチェック
+if ! task fmt >/dev/null 2>&1; then
+    echo "❌ フォーマットエラーが検出されました"
+    exit 1
+fi
+
+# 静的解析
+if command -v golangci-lint >/dev/null 2>&1; then
+    if ! task lint >/dev/null 2>&1; then
+        echo "❌ 静的解析エラーが検出されました"
+        exit 1
+    fi
+else
+    echo "⚠️  golangci-lintがインストールされていません"
+fi
+
+# テスト実行
+if ! task test >/dev/null 2>&1; then
+    echo "❌ テストが失敗しました"
+    exit 1
+fi
+
+echo "✅ 品質チェック完了 - コミット可能です"
+exit 0
diff --git a/app/Taskfile.yml b/app/Taskfile.yml
new file mode 100644
index 0000000..e61c4b4
--- /dev/null
+++ b/app/Taskfile.yml
@@ -0,0 +1,101 @@
+version: '3'
+
+vars:
+  BINARY_NAME: go-tdd-tutorial
+  BUILD_DIR: bin
+
+tasks:
+  default:
+    desc: ヘルプを表示
+    cmds:
+      - task --list
+
+  test:
+    desc: テストを実行
+    cmds:
+      - echo "テストを実行中..."
+      - go test -v ./...
+
+  test-coverage:
+    desc: テストカバレッジを実行
+    cmds:
+      - echo "テストカバレッジを実行中..."
+      - go test -coverprofile=coverage.out ./...
+      - go tool cover -html=coverage.out -o coverage.html
+      - echo "カバレッジレポートが coverage.html に作成されました"
+
+  lint:
+    desc: 静的解析を実行
+    cmds:
+      - echo "静的解析を実行中..."
+      - cmd: command -v golangci-lint >/dev/null 2>&1
+        ignore_error: true
+      - export PATH=$PATH:$(go env GOPATH)/bin && golangci-lint run
+
+  fmt:
+    desc: コードフォーマットを実行
+    cmds:
+      - echo "コードフォーマットを実行中..."
+      - go fmt ./...
+
+  vet:
+    desc: go vetを実行
+    cmds:
+      - echo "go vetを実行中..."
+      - go vet ./...
+
+  build:
+    desc: アプリケーションをビルド
+    cmds:
+      - echo "アプリケーションをビルド中..."
+      - mkdir -p {{.BUILD_DIR}}
+      - go build -o {{.BUILD_DIR}}/{{.BINARY_NAME}} .
+
+  run:
+    desc: アプリケーションを実行
+    cmds:
+      - echo "アプリケーションを実行中..."
+      - go run .
+
+  clean:
+    desc: クリーンアップ
+    cmds:
+      - echo "クリーンアップ中..."
+      - go clean
+      - rm -rf {{.BUILD_DIR}}
+      - rm -f coverage.out coverage.html
+
+  check:
+    desc: 品質チェック（フォーマット、vet、lint、テスト）
+    deps: [fmt, vet, lint, test]
+
+  fix:
+    desc: 自動修正（フォーマット）
+    deps: [fmt]
+
+  deps:
+    desc: 依存関係のダウンロード
+    cmds:
+      - echo "依存関係をダウンロード中..."
+      - go mod download
+      - go mod tidy
+
+  watch:
+    desc: ファイル監視による自動チェック
+    cmds:
+      - echo "ファイル監視による自動チェックを開始..."
+      - task: watch-test
+
+  watch-test:
+    desc: テストのwatch実行
+    cmds:
+      - echo "テストのwatch実行を開始..."
+      - |
+        if command -v fswatch >/dev/null 2>&1; then
+          fswatch -o . | xargs -n1 -I{} task test
+        else
+          echo "fswatch がインストールされていません。"
+          echo "Ubuntu/Debian: sudo apt-get install fswatch"
+          echo "macOS: brew install fswatch"
+          exit 1
+        fi
diff --git a/app/setup-hooks.sh b/app/setup-hooks.sh
new file mode 100644
index 0000000..29f4a5c
--- /dev/null
+++ b/app/setup-hooks.sh
@@ -0,0 +1,22 @@
+#!/bin/bash
+# Gitフックをセットアップするスクリプト
+
+echo "Gitフックをセットアップ中..."
+
+# プロジェクトルートディレクトリに移動
+cd "$(dirname "$0")"
+
+# .git/hooksディレクトリが存在しない場合は作成
+if [ ! -d ".git/hooks" ]; then
+    echo ".git/hooksディレクトリが見つかりません。Gitリポジトリを初期化してください。"
+    exit 1
+fi
+
+# pre-commitフックをコピー
+cp .githooks/pre-commit .git/hooks/pre-commit
+
+# 実行権限を付与
+chmod +x .git/hooks/pre-commit
+
+echo "✅ Gitフックのセットアップが完了しました"
+echo "コミット前に自動で品質チェックが実行されます"

```

## コミット: 1219439

### メッセージ

```
chore: コードカバレッジセットアップ
```

### 変更されたファイル

- A	app/coverage.html
- A	app/coverage.out

### 変更内容

```diff
commit 12194395bb698e80284379e2763980ac715da5a6
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 04:52:54 2025 +0000

    chore: コードカバレッジセットアップ

diff --git a/app/coverage.html b/app/coverage.html
new file mode 100644
index 0000000..2d8d786
--- /dev/null
+++ b/app/coverage.html
@@ -0,0 +1,138 @@
+
+<!DOCTYPE html>
+<html>
+	<head>
+		<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
+		<title>go-tdd-tutorial: Go Coverage Report</title>
+		<style>
+			body {
+				background: black;
+				color: rgb(80, 80, 80);
+			}
+			body, pre, #legend span {
+				font-family: Menlo, monospace;
+				font-weight: bold;
+			}
+			#topbar {
+				background: black;
+				position: fixed;
+				top: 0; left: 0; right: 0;
+				height: 42px;
+				border-bottom: 1px solid rgb(80, 80, 80);
+			}
+			#content {
+				margin-top: 50px;
+			}
+			#nav, #legend {
+				float: left;
+				margin-left: 10px;
+			}
+			#legend {
+				margin-top: 12px;
+			}
+			#nav {
+				margin-top: 10px;
+			}
+			#legend span {
+				margin: 0 5px;
+			}
+			.cov0 { color: rgb(192, 0, 0) }
+.cov1 { color: rgb(128, 128, 128) }
+.cov2 { color: rgb(116, 140, 131) }
+.cov3 { color: rgb(104, 152, 134) }
+.cov4 { color: rgb(92, 164, 137) }
+.cov5 { color: rgb(80, 176, 140) }
+.cov6 { color: rgb(68, 188, 143) }
+.cov7 { color: rgb(56, 200, 146) }
+.cov8 { color: rgb(44, 212, 149) }
+.cov9 { color: rgb(32, 224, 152) }
+.cov10 { color: rgb(20, 236, 155) }
+
+		</style>
+	</head>
+	<body>
+		<div id="topbar">
+			<div id="nav">
+				<select id="files">
+				
+				<option value="file0">go-tdd-tutorial/main.go (73.3%)</option>
+				
+				</select>
+			</div>
+			<div id="legend">
+				<span>not tracked</span>
+			
+				<span class="cov0">not covered</span>
+				<span class="cov8">covered</span>
+			
+			</div>
+		</div>
+		<div id="content">
+		
+		<pre class="file" id="file0" style="display: none">package main
+
+import (
+        "fmt"
+        "strconv"
+)
+
+func Generate(number int) string <span class="cov8" title="1">{
+        if number%3 == 0 &amp;&amp; number%5 == 0 </span><span class="cov8" title="1">{
+                return "FizzBuzz"
+        }</span>
+        <span class="cov8" title="1">if number%3 == 0 </span><span class="cov8" title="1">{
+                return "Fizz"
+        }</span>
+        <span class="cov8" title="1">if number%5 == 0 </span><span class="cov8" title="1">{
+                return "Buzz"
+        }</span>
+        <span class="cov8" title="1">return strconv.Itoa(number)</span>
+}
+
+func GenerateList(start, end int) []string <span class="cov8" title="1">{
+        results := make([]string, 0, end-start+1)
+        for i := start; i &lt;= end; i++ </span><span class="cov8" title="1">{
+                results = append(results, Generate(i))
+        }</span>
+        <span class="cov8" title="1">return results</span>
+}
+
+func main() <span class="cov0" title="0">{
+        fmt.Println("FizzBuzz Game:")
+        results := GenerateList(1, 100)
+        for _, result := range results </span><span class="cov0" title="0">{
+                fmt.Println(result)
+        }</span>
+}
+</pre>
+		
+		</div>
+	</body>
+	<script>
+	(function() {
+		var files = document.getElementById('files');
+		var visible;
+		files.addEventListener('change', onChange, false);
+		function select(part) {
+			if (visible)
+				visible.style.display = 'none';
+			visible = document.getElementById(part);
+			if (!visible)
+				return;
+			files.value = part;
+			visible.style.display = 'block';
+			location.hash = part;
+		}
+		function onChange() {
+			select(files.value);
+			window.scrollTo(0, 0);
+		}
+		if (location.hash != "") {
+			select(location.hash.substr(1));
+		}
+		if (!visible) {
+			select("file0");
+		}
+	})();
+	</script>
+</html>
diff --git a/app/coverage.out b/app/coverage.out
new file mode 100644
index 0000000..fe5dc7c
--- /dev/null
+++ b/app/coverage.out
@@ -0,0 +1,13 @@
+mode: set
+go-tdd-tutorial/main.go:8.34,9.36 1 1
+go-tdd-tutorial/main.go:9.36,11.3 1 1
+go-tdd-tutorial/main.go:12.2,12.19 1 1
+go-tdd-tutorial/main.go:12.19,14.3 1 1
+go-tdd-tutorial/main.go:15.2,15.19 1 1
+go-tdd-tutorial/main.go:15.19,17.3 1 1
+go-tdd-tutorial/main.go:18.2,18.29 1 1
+go-tdd-tutorial/main.go:21.44,23.32 2 1
+go-tdd-tutorial/main.go:23.32,25.3 1 1
+go-tdd-tutorial/main.go:26.2,26.16 1 1
+go-tdd-tutorial/main.go:29.13,32.33 3 0
+go-tdd-tutorial/main.go:32.33,34.3 1 0

```

## コミット: 58f34e5

### メッセージ

```
chore: 静的コード解析セットアップ
```

### 変更されたファイル

- A	app/.golangci.yml
- M	app/go.mod
- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 58f34e555653ee5d6f24c0ea2dd282b06e0ba56e
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 04:51:53 2025 +0000

    chore: 静的コード解析セットアップ

diff --git a/app/.golangci.yml b/app/.golangci.yml
new file mode 100644
index 0000000..987670a
--- /dev/null
+++ b/app/.golangci.yml
@@ -0,0 +1,46 @@
+run:
+  timeout: 5m
+  issues-exit-code: 1
+  tests: true
+
+output:
+  formats:
+    - format: colored-line-number
+  print-issued-lines: true
+  print-linter-name: true
+
+linters-settings:
+  gocyclo:
+    min-complexity: 10
+  misspell:
+    locale: US
+  lll:
+    line-length: 120
+  gofmt:
+    simplify: true
+
+linters:
+  enable:
+    - gosimple
+    - govet
+    - ineffassign
+    - misspell
+    - gofmt
+    - goimports
+    - gocyclo
+    - gosec
+    - unconvert
+    - unused
+    - staticcheck
+    - typecheck
+  disable:
+    - errcheck
+
+issues:
+  exclude-rules:
+    - path: _test\.go
+      linters:
+        - gosec
+    - text: "has \\d+ occurrences, make it a constant"
+      linters:
+        - goconst
diff --git a/app/go.mod b/app/go.mod
index 9a69f38..1583b43 100644
--- a/app/go.mod
+++ b/app/go.mod
@@ -1,3 +1,3 @@
-module fizzbuzz
+module go-tdd-tutorial
 
 go 1.22.0
diff --git a/app/main.go b/app/main.go
index 34664f7..bb7027d 100644
--- a/app/main.go
+++ b/app/main.go
@@ -1,35 +1,35 @@
 package main
 
 import (
-    "fmt"
-    "strconv"
+	"fmt"
+	"strconv"
 )
 
 func Generate(number int) string {
-    if number%3 == 0 && number%5 == 0 {
-        return "FizzBuzz"
-    }
-    if number%3 == 0 {
-        return "Fizz"
-    }
-    if number%5 == 0 {
-        return "Buzz"
-    }
-    return strconv.Itoa(number)
+	if number%3 == 0 && number%5 == 0 {
+		return "FizzBuzz"
+	}
+	if number%3 == 0 {
+		return "Fizz"
+	}
+	if number%5 == 0 {
+		return "Buzz"
+	}
+	return strconv.Itoa(number)
 }
 
 func GenerateList(start, end int) []string {
-    results := make([]string, 0, end-start+1)
-    for i := start; i <= end; i++ {
-        results = append(results, Generate(i))
-    }
-    return results
+	results := make([]string, 0, end-start+1)
+	for i := start; i <= end; i++ {
+		results = append(results, Generate(i))
+	}
+	return results
 }
 
 func main() {
-    fmt.Println("FizzBuzz Game:")
-    results := GenerateList(1, 100)
-    for _, result := range results {
-        fmt.Println(result)
-    }
+	fmt.Println("FizzBuzz Game:")
+	results := GenerateList(1, 100)
+	for _, result := range results {
+		fmt.Println(result)
+	}
 }
diff --git a/app/main_test.go b/app/main_test.go
index 45a9374..0b2d3dd 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -3,60 +3,60 @@ package main
 import "testing"
 
 func assertGenerate(t *testing.T, input int, expected string) {
-    t.Helper()
-    got := Generate(input)
-    if got != expected {
-        t.Errorf("Generate(%d) = %v, want %v", input, got, expected)
-    }
+	t.Helper()
+	got := Generate(input)
+	if got != expected {
+		t.Errorf("Generate(%d) = %v, want %v", input, got, expected)
+	}
 }
 
 func Test1を渡したら文字列1を返す(t *testing.T) {
-    assertGenerate(t, 1, "1")
+	assertGenerate(t, 1, "1")
 }
 
 func Test2を渡したら文字列2を返す(t *testing.T) {
-    assertGenerate(t, 2, "2")
+	assertGenerate(t, 2, "2")
 }
 
 func Test3を渡したらFizzを返す(t *testing.T) {
-    assertGenerate(t, 3, "Fizz")
+	assertGenerate(t, 3, "Fizz")
 }
 
 func Test6を渡したらFizzを返す(t *testing.T) {
-    assertGenerate(t, 6, "Fizz")
+	assertGenerate(t, 6, "Fizz")
 }
 
 func Test5を渡したらBuzzを返す(t *testing.T) {
-    assertGenerate(t, 5, "Buzz")
+	assertGenerate(t, 5, "Buzz")
 }
 
 func Test10を渡したらBuzzを返す(t *testing.T) {
-    assertGenerate(t, 10, "Buzz")
+	assertGenerate(t, 10, "Buzz")
 }
 
 func Test15を渡したらFizzBuzzを返す(t *testing.T) {
-    assertGenerate(t, 15, "FizzBuzz")
+	assertGenerate(t, 15, "FizzBuzz")
 }
 
 func Test1から100までのFizzBuzzを返す(t *testing.T) {
-    results := GenerateList(1, 100)
-    
-    // いくつかの値を確認
-    if len(results) != 100 {
-        t.Errorf("Length = %d, want 100", len(results))
-    }
-    
-    // 具体的な値を確認
-    if results[0] != "1" {
-        t.Errorf("results[0] = %s, want 1", results[0])
-    }
-    if results[2] != "Fizz" {
-        t.Errorf("results[2] = %s, want Fizz", results[2])
-    }
-    if results[4] != "Buzz" {
-        t.Errorf("results[4] = %s, want Buzz", results[4])
-    }
-    if results[14] != "FizzBuzz" {
-        t.Errorf("results[14] = %s, want FizzBuzz", results[14])
-    }
+	results := GenerateList(1, 100)
+
+	// いくつかの値を確認
+	if len(results) != 100 {
+		t.Errorf("Length = %d, want 100", len(results))
+	}
+
+	// 具体的な値を確認
+	if results[0] != "1" {
+		t.Errorf("results[0] = %s, want 1", results[0])
+	}
+	if results[2] != "Fizz" {
+		t.Errorf("results[2] = %s, want Fizz", results[2])
+	}
+	if results[4] != "Buzz" {
+		t.Errorf("results[4] = %s, want Buzz", results[4])
+	}
+	if results[14] != "FizzBuzz" {
+		t.Errorf("results[14] = %s, want FizzBuzz", results[14])
+	}
 }

```

## コミット: b093566

### メッセージ

```
feat: 1から100までのFizzBuzz出力機能を完成
```

### 変更されたファイル

- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit b0935668a1e82d51b46c43d59ae2645f74ee7bef
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 04:44:39 2025 +0000

    feat: 1から100までのFizzBuzz出力機能を完成

diff --git a/app/main.go b/app/main.go
index a54def6..34664f7 100644
--- a/app/main.go
+++ b/app/main.go
@@ -1,7 +1,35 @@
 package main
 
-import "strconv"
+import (
+    "fmt"
+    "strconv"
+)
 
 func Generate(number int) string {
+    if number%3 == 0 && number%5 == 0 {
+        return "FizzBuzz"
+    }
+    if number%3 == 0 {
+        return "Fizz"
+    }
+    if number%5 == 0 {
+        return "Buzz"
+    }
     return strconv.Itoa(number)
 }
+
+func GenerateList(start, end int) []string {
+    results := make([]string, 0, end-start+1)
+    for i := start; i <= end; i++ {
+        results = append(results, Generate(i))
+    }
+    return results
+}
+
+func main() {
+    fmt.Println("FizzBuzz Game:")
+    results := GenerateList(1, 100)
+    for _, result := range results {
+        fmt.Println(result)
+    }
+}
diff --git a/app/main_test.go b/app/main_test.go
index b5ffeb6..45a9374 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -17,3 +17,46 @@ func Test1を渡したら文字列1を返す(t *testing.T) {
 func Test2を渡したら文字列2を返す(t *testing.T) {
     assertGenerate(t, 2, "2")
 }
+
+func Test3を渡したらFizzを返す(t *testing.T) {
+    assertGenerate(t, 3, "Fizz")
+}
+
+func Test6を渡したらFizzを返す(t *testing.T) {
+    assertGenerate(t, 6, "Fizz")
+}
+
+func Test5を渡したらBuzzを返す(t *testing.T) {
+    assertGenerate(t, 5, "Buzz")
+}
+
+func Test10を渡したらBuzzを返す(t *testing.T) {
+    assertGenerate(t, 10, "Buzz")
+}
+
+func Test15を渡したらFizzBuzzを返す(t *testing.T) {
+    assertGenerate(t, 15, "FizzBuzz")
+}
+
+func Test1から100までのFizzBuzzを返す(t *testing.T) {
+    results := GenerateList(1, 100)
+    
+    // いくつかの値を確認
+    if len(results) != 100 {
+        t.Errorf("Length = %d, want 100", len(results))
+    }
+    
+    // 具体的な値を確認
+    if results[0] != "1" {
+        t.Errorf("results[0] = %s, want 1", results[0])
+    }
+    if results[2] != "Fizz" {
+        t.Errorf("results[2] = %s, want Fizz", results[2])
+    }
+    if results[4] != "Buzz" {
+        t.Errorf("results[4] = %s, want Buzz", results[4])
+    }
+    if results[14] != "FizzBuzz" {
+        t.Errorf("results[14] = %s, want FizzBuzz", results[14])
+    }
+}

```

## コミット: d8dd359

### メッセージ

```
refactor: メソッドの抽出
```

### 変更されたファイル

- M	app/main_test.go

### 変更内容

```diff
commit d8dd359d0524df04cc12f1418dbc94416c28b3e2
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 04:38:29 2025 +0000

    refactor: メソッドの抽出

diff --git a/app/main_test.go b/app/main_test.go
index 44238a1..b5ffeb6 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -2,18 +2,18 @@ package main
 
 import "testing"
 
-func Test1を渡したら文字列1を返す(t *testing.T) {
-    got := Generate(1)
-    want := "1"
-    if got != want {
-        t.Errorf("Generate(1) = %v, want %v", got, want)
+func assertGenerate(t *testing.T, input int, expected string) {
+    t.Helper()
+    got := Generate(input)
+    if got != expected {
+        t.Errorf("Generate(%d) = %v, want %v", input, got, expected)
     }
 }
 
+func Test1を渡したら文字列1を返す(t *testing.T) {
+    assertGenerate(t, 1, "1")
+}
+
 func Test2を渡したら文字列2を返す(t *testing.T) {
-    got := Generate(2)
-    want := "2"
-    if got != want {
-        t.Errorf("Generate(2) = %v, want %v", got, want)
-    }
+    assertGenerate(t, 2, "2")
 }

```

## コミット: 305720e

### メッセージ

```
test: 数を文字列にして返す
```

### 変更されたファイル

- M	app/main.go
- M	app/main_test.go

### 変更内容

```diff
commit 305720ed00da7528bed553138c38aa3380238ae7
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 04:37:48 2025 +0000

    test: 数を文字列にして返す

diff --git a/app/main.go b/app/main.go
index 8091d7d..a54def6 100644
--- a/app/main.go
+++ b/app/main.go
@@ -1,11 +1,7 @@
 package main
 
-import "fmt"
+import "strconv"
 
-func greeting() string {
-    return "hello world"
-}
-
-func main() {
-    fmt.Println(greeting())
+func Generate(number int) string {
+    return strconv.Itoa(number)
 }
diff --git a/app/main_test.go b/app/main_test.go
index b67e072..44238a1 100644
--- a/app/main_test.go
+++ b/app/main_test.go
@@ -2,10 +2,18 @@ package main
 
 import "testing"
 
-func TestGreeting(t *testing.T) {
-    got := greeting()
-    want := "hello world"
+func Test1を渡したら文字列1を返す(t *testing.T) {
+    got := Generate(1)
+    want := "1"
     if got != want {
-        t.Errorf("greeting() = %v, want %v", got, want)
+        t.Errorf("Generate(1) = %v, want %v", got, want)
+    }
+}
+
+func Test2を渡したら文字列2を返す(t *testing.T) {
+    got := Generate(2)
+    want := "2"
+    if got != want {
+        t.Errorf("Generate(2) = %v, want %v", got, want)
     }
 }

```

## コミット: ad5c413

### メッセージ

```
test: セットアップ
```

### 変更されたファイル

- A	app/go.mod
- A	app/main.go
- A	app/main_test.go

### 変更内容

```diff
commit ad5c413babb62ff42245bb0a6cf047882f8b4680
Author: k2works <kakimomokuri@gmail.com>
Date:   Thu Jul 10 04:31:45 2025 +0000

    test: セットアップ

diff --git a/app/go.mod b/app/go.mod
new file mode 100644
index 0000000..9a69f38
--- /dev/null
+++ b/app/go.mod
@@ -0,0 +1,3 @@
+module fizzbuzz
+
+go 1.22.0
diff --git a/app/main.go b/app/main.go
new file mode 100644
index 0000000..8091d7d
--- /dev/null
+++ b/app/main.go
@@ -0,0 +1,11 @@
+package main
+
+import "fmt"
+
+func greeting() string {
+    return "hello world"
+}
+
+func main() {
+    fmt.Println(greeting())
+}
diff --git a/app/main_test.go b/app/main_test.go
new file mode 100644
index 0000000..b67e072
--- /dev/null
+++ b/app/main_test.go
@@ -0,0 +1,11 @@
+package main
+
+import "testing"
+
+func TestGreeting(t *testing.T) {
+    got := greeting()
+    want := "hello world"
+    if got != want {
+        t.Errorf("greeting() = %v, want %v", got, want)
+    }
+}

```

