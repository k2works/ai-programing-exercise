# 作業履歴 2025-07-02

## 概要

2025-07-02の作業内容をまとめています。

## コミット: 4ed036f

### メッセージ

```
chore: update Dockerfile to install Leiningen and add .cpcache to .gitignore
```

### 変更されたファイル

- M	.gitignore
- M	Dockerfile

### 変更内容

```diff
commit 4ed036fd235fe3b001e3f6caa2bbf2718cca73f4
Author: k2works <kakimomokuri@gmail.com>
Date:   Wed Jul 2 15:17:35 2025 +0900

    chore: update Dockerfile to install Leiningen and add .cpcache to .gitignore

diff --git a/.gitignore b/.gitignore
index 18196d5..f6e763f 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,3 +1,4 @@
 .idea
 site
-node_modules
\ No newline at end of file
+node_modules
+.cpcache
\ No newline at end of file
diff --git a/Dockerfile b/Dockerfile
index 1b2481d..2a8c178 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -53,7 +53,10 @@ RUN apt-get update && apt-get install -y curl bash rlwrap && apt-get clean \
     && curl -L -O https://github.com/clojure/brew-install/releases/latest/download/linux-install.sh \
     && chmod +x linux-install.sh \
     && ./linux-install.sh \
-    && rm linux-install.sh
+    && rm linux-install.sh \
+    && bash -c 'source "$HOME/.sdkman/bin/sdkman-init.sh" \
+       && sdk selfupdate \
+       && sdk install leiningen "$LEININGEN_VER"'
 
 # Node.jsのインストール
 RUN curl -fsSL https://deb.nodesource.com/setup_18.x | bash - \

```

