# 作業履歴 2025-07-13

## 概要

2025-07-13の作業内容をまとめています。

## コミット: 60dc6cf

### メッセージ

```
chore: update Docker image version to 0.0.9 in docker-compose.yml
```

### 変更されたファイル

- M	docker-compose.yml

### 変更内容

```diff
commit 60dc6cf0ff1fa9fba8c88049381053dca52fc5a5
Author: k2works <kakimomokuri@gmail.com>
Date:   Sun Jul 13 20:41:43 2025 +0900

    chore: update Docker image version to 0.0.9 in docker-compose.yml

diff --git a/docker-compose.yml b/docker-compose.yml
index 9eded97..d115163 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -1,6 +1,6 @@
 services:
   app:
-    image: ghcr.io/k2works/ai-programing-exercise/core:0.0.8
+    image: ghcr.io/k2works/ai-programing-exercise/core:0.0.9
     volumes:
       - ./:/srv/app
       - ~/.ssh/id_rsa:/root/.ssh/id_rsa

```

## コミット: 750cfed

### メッセージ

```
chore: add .NET SDK 8.0 installation to Dockerfile and update README for .NET support
```

### 変更されたファイル

- M	Dockerfile
- M	README.md

### 変更内容

```diff
commit 750cfeda15dbe33f77da2c78729c1fa35855eac4
Author: k2works <kakimomokuri@gmail.com>
Date:   Sun Jul 13 20:39:53 2025 +0900

    chore: add .NET SDK 8.0 installation to Dockerfile and update README for .NET support

diff --git a/Dockerfile b/Dockerfile
index e78e828..2f2f3e1 100644
--- a/Dockerfile
+++ b/Dockerfile
@@ -160,6 +160,18 @@ RUN apt-get update && apt-get install -y \
     && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain $RUST_VER \
     && echo 'source $HOME/.cargo/env' >> ~/.bashrc
 
+# .NET SDKのインストール
+RUN apt-get update && apt-get install -y \
+    wget \
+    apt-transport-https \
+    && wget https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb -O packages-microsoft-prod.deb \
+    && dpkg -i packages-microsoft-prod.deb \
+    && rm packages-microsoft-prod.deb \
+    && apt-get update \
+    && apt-get install -y dotnet-sdk-8.0 \
+    && apt-get clean \
+    && rm -rf /var/lib/apt/lists/*
+
 # Gemini CLIのインストール
 RUN npm install -g @google/gemini-cli
 
@@ -167,7 +179,7 @@ RUN npm install -g @google/gemini-cli
 RUN npm install -g @anthropic-ai/claude-code
 
 # パスの設定
-ENV PATH="/root/.cargo/bin:/usr/local/go/bin:/root/.ghcup/bin:/root/.sdkman/candidates/java/current/bin:/root/.sdkman/candidates/maven/current/bin:/root/.sdkman/candidates/gradle/current/bin:/root/.rbenv/shims:/usr/local/bin:$PATH"
+ENV PATH="/root/.cargo/bin:/usr/local/go/bin:/root/.ghcup/bin:/root/.sdkman/candidates/java/current/bin:/root/.sdkman/candidates/maven/current/bin:/root/.sdkman/candidates/gradle/current/bin:/root/.rbenv/shims:/usr/share/dotnet:/usr/share/dotnet/tools:/usr/local/bin:$PATH"
 
 # 作業ディレクトリの設定
 WORKDIR /srv
diff --git a/README.md b/README.md
index e5f25cd..44b54d2 100644
--- a/README.md
+++ b/README.md
@@ -2,12 +2,12 @@
 
 ## 概要
 
-AIプログラミングの実践的な学習と開発のためのプロジェクトです。複数のプログラミング言語（Java、Node.js、Ruby、Python、PHP、Go、Rust）をサポートする開発環境と、MkDocsを使用したドキュメント管理システムを提供します。
+AIプログラミングの実践的な学習と開発のためのプロジェクトです。複数のプログラミング言語（Java、Node.js、Ruby、Python、PHP、Go、Rust、.NET）をサポートする開発環境と、MkDocsを使用したドキュメント管理システムを提供します。
 
 ### 目的
 
 - AIプログラミングの実践的な学習環境の提供
-- 複数言語（Java、Node.js、Ruby、Python、PHP、Go、Rust）での開発サポート
+- 複数言語（Java、Node.js、Ruby、Python、PHP、Go、Rust、.NET）での開発サポート
 - ドキュメント駆動開発の促進
 - プログラミング学習リソースの集約
 
@@ -66,6 +66,7 @@ AIプログラミングの実践的な学習と開発のためのプロジェク
   - Haskell 9.4.8 (via GHCup)
   - Go 1.22.0 (via 公式バイナリ)
   - Rust stable (via rustup)
+  - .NET 8.0 (via Microsoft パッケージリポジトリ)
 - **ドキュメント環境**:
   - MkDocs (Python 3.11ベース)
   - Material for MkDocsテーマ
@@ -235,6 +236,7 @@ echo $GITHUB_TOKEN | docker login ghcr.io -u <username> --password-stdin
    - Haskell: `ghc --version`
    - Go: `go version`
    - Rust: `rustc --version`
+   - .NET: `dotnet --version`
 
 #### ドキュメント作成
 

```

## コミット: 87bec9a

### メッセージ

```
chore: update .gitignore to include vendor directory and ensure .env is ignored
```

### 変更されたファイル

- M	.gitignore

### 変更内容

```diff
commit 87bec9afe3d47f1fb0335c89b0173d68c25293e6
Author: k2works <kakimomokuri@gmail.com>
Date:   Sun Jul 13 09:02:13 2025 +0900

    chore: update .gitignore to include vendor directory and ensure .env is ignored

diff --git a/.gitignore b/.gitignore
index 6d157fe..9788b14 100644
--- a/.gitignore
+++ b/.gitignore
@@ -6,4 +6,5 @@ coverage
 build
 .gradle
 target
-.env
\ No newline at end of file
+.env
+vendor
\ No newline at end of file

```

